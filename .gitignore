# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# The virtualenv scripts/create_venv.sh creates, at the repo root.  Without
# this, running that script and then `git add -A` stages several thousand files.
.venv/

# Test / lint caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage

# Training and benchmark artifacts
*.pt
tiny-transformer.pt/
artifacts/

# macOS
.DS_Store

# Vendored reference checkouts (Megatron-LM / DeepSpeed / Ulysses).
# Read-only implementation references: never packaged, and
# scripts/check_no_reference_imports.py fails the build if any src import
# reaches into this directory.
references/
