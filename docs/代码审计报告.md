# aiTrainer 代码审计报告：实现缺陷与可用性缺口

> 审计对象：`aiTrainer`（`src/aitrainer`，约 6,360 LOC / 66 文件）
> 审计方式：静态阅读 + **真实 torch 实跑验证**（多智能体分块审计，关键结论由审计者逐条复核）
> 结论一句话：**工程纪律真诚，但核心分布式能力从未被执行过一次。仓库自己的测试套件在装上 torch 后立刻红 10 个，TP 与 SP 在任何 world size 下都抛异常。**

---

## 解耦与分层重构（2026-09-16）

审计之后做了一轮结构重构，共 9 个阶段、9 个 commit（`0b28273` → `84c3fd8`），每阶段独立验收。**它没有改变任何行为**——除下面标注的两处刻意行为变更外，测试数只增不减。

| 阶段 | 内容 | 关键证据 |
|---|---|---|
| 0 | `git init` + 基线提交 | 此前**不是 git 仓库也没有父仓库**，"分阶段可回退"本来是空话 |
| 1 | import-linter 棘轮 | 按当前状态声明使其当天即通过；已验证**能失败**（塞一个向上导入 → `parallel.tp -> trainer (l.4)`） |
| 2 | 删死代码 + 惰性配置 | 5 个模块、~14 个符号、16 个惰性字段；`CheckpointConfig` 两字段都空后整个类连 `FrameworkConfig.checkpoint` 一起删 |
| 3 | 抽出 `core/` 叶子层 + 去重 | 4 个 world-size 辅助、12 处 dtype 归一、~15 处字节数计算、2 个遍历器、4 份 loss → 各 1 份；`ignore_imports` 清空 |
| 4 | **补生产 PP 路径的多卡覆盖（只加测试）** | 发现 `PipelineStage` 在 pp>1 **从未被执行过一次**；新增 case 证明生产路径**本来就是对的**（`max\|diff\| = 0.000e+00`） |
| 5 | 流水线调度合一 | 四份实现 → 一份；`distributed_model.py` 350 → 145 行 |
| 6 | `trainer.py` 拆包 | 447 行 / ~10 个关注点 → 7 个模块；**门面直接持有状态**，避免代理使 `trainer.loss_fn = f` 静默失效 |
| 7 | `config/` 拆包 + 校验分层 | 嵌套配置从类型注解反射，不再硬编码特例；`Trainer` 构造校验次数 2 → 1 |
| 8 | 契约收紧 + 文档拆分 + API 面收口 | 3 条契约、0 违反、ignore 为空；`__all__` 183（含 27 个泄漏子模块名）→ 142 个策展名 |

**验收对比**

| 指标 | 重构前 | 重构后 |
|---|---|---|
| `pytest -q` | 183 passed | **194 passed** |
| `ruff check` | 143 | **91** |
| `lint-imports` | 不存在 | **3 契约 kept / 0 broken，ignore 为空** |
| 示例 | 7/7 | **7/7**（含 2 rank FSDP） |
| `aitrainer.__all__` | 183（27 个是子模块名） | **142 个策展名** |
| `src/aitrainer` | 64 文件 / 7,019 行 | 74 文件 / 7,468 行 |

**两处刻意的行为变更**（其余为纯搬家）：
1. `FSDPWrapper` 对「`forward_prefetch` 需要已 finalize 的 trace」改为**抛错**，原先静默把标志按位与掉——「被拒绝」和「关着」是两回事。
   （**已失效**：Stage A 迁移到 FSDP2 后 `FSDPWrapper` 与这两个字段都不存在了，见下节。）
2. `Trainer.__init__` 不再重复校验同一份配置（原先 `config.validate` 跑两遍，`from_model` 路径三遍）。

**重构中被测试网捞到的错**（都不是重构引入的，是覆盖补上后暴露的）：
- **GPipe 上报的 loss 少一半**：梯度正确、只有上报值被 `denom` 除错。没有 Stage 4 的覆盖，这会以「loss 看着正常但小一倍」混过去。
- **1F1B 首级丢失 early return**：17 项协议测试**在带 bug 时全绿**，唯一信号是一条被当作噪音的 PyTorch `.grad` 警告。
- **Stage 7 的重复异常类**：拆分时替换静默未匹配，`schema.py` 留了个本地 `ConfigurationError`，与包重导出的是两个类，`except ConfigurationError` 不再捕获 —— 18 个测试红。
- **Stage 3 的迁移脚本中途崩**导致四个文件未迁移，而 **181 个测试照样全绿** —— 由此发现三条异步集合通信（`all_reduce_async` 等）从未被执行过，已删除。

**明确未做**（用户决定或已记录的取舍）：
- `ruff` 的 91 个既有错误**不动，只记录**（其中 6 条随 `lifecycle.py` 搬入 `core/`）。
- `kernels/` 本轮不碰；`docs/架构.md` 里如实标注它**没有任何生产调用方**。
- 高级 overlap 开关保留为**仅拒绝用**，删除它们等于悄悄放宽可接受配置空间。
- `api.py` 的 `__all__` 未能按层分组：ruff 的 RUF022 要求排序，两者冲突，已在 docstring 写明取舍而非用 `# noqa` 压过。

---

## FSDP2 迁移（2026-09-16，Stage A 已完成）

诉求是**一套代码里不要混着两代 FSDP 语义**。Stage A 把 `parallel/fsdp.py` 整体换成
FSDP2（`torch.distributed.fsdp.fully_shard`），并删掉 FSDP1 没有对应物的东西。

**删除**：`FSDPWrapper`、`fsdp_available()`（零调用方）、`full_state_dict_context()`、
`FSDPConfig` 的 `sharding`/`limit_all_gathers`/`forward_prefetch`/`execution_trace_complete`/
`cpu_offload`（5 个字段，全部到不了任何地方——设成 true 也既不切分也不预取）。

**替换**：

| FSDP1 | FSDP2 | 写错的后果 |
|---|---|---|
| `module.no_sync()` | `set_requires_gradient_sync(False)` | 每个 microbatch 一次完整归约：更慢、归约时机不同，**不报错** |
| `module.clip_grad_norm_` | `parallel.fsdp.clip_grad_norm_`（自己归约） | FSDP2 无此方法；`torch.distributed.fsdp.clip_grad_norm_` 在 2.9 **不存在**。看似等价的 `nn.utils.clip_grad_norm_` 会**静默给出分片范数**，见下 |
| `full_state_dict_context()` | `get_model_state_dict(..., StateDictOptions(full_state_dict=True))` | 默认返回的是 `DTensor`，`full_tensor()` 才拿到全局张量 |
| `FullyShardedDataParallel(m)` | `fully_shard(m)`（就地，返回同一对象） | 实例的类被换成 `FSDPLinear`：`type(x).__name__` 变了但 `x is module` 仍为真 |

**本轮实测出来的、文档里没有的语义**（`tests/multirank/worker.py`
的 `fsdp_accumulation_window_suppresses_reduction`）：累积窗口内 FSDP2 **完全不物化
`.grad`**（一直是 `None`，缓存在内部），下一个**同步**的 backward 才把全部累积一次性
归约——实测正好是 `4 * unit` 而不是 `1 * unit`。两个推论：

1. 窗口中途做 optimizer step 是**静默无效**的（`.grad` 是 `None`）。`fit` 的「只在该
   边界步进」不变式因此不只是性能问题，是正确性问题。
2. 原来那条多卡回归 `trainer_fit_fsdp_accumulation` **抓不到** `no_sync` 失效：两个
   rank 喂的是同一份数据，于是「每个 microbatch 各归约一次」和「边界归约一次」的和
   完全相同。新 case 因此使用**两个 rank 不同的数据**，并显式断言「归约值 ≠ 本 rank
   局部值」，否则整条测试是空转的。

**反向对照**：把 `no_gradient_sync` 还原成静默 no-op，新测试立刻变红
（`materialised_inside_window` 从 `[False, False, False]` 变成 `[True, True, True]`）。

**本轮靠「验证自己写下的注释」抓到的缺陷（`fsdp_grad_norm_is_global`）**：我先把
「`nn.utils.clip_grad_norm_` 在 DTensor 上算的是全局范数」写进了注释和文档，然后去
实测这句话——**它是错的**。DTensor 确实注册了 `linalg.vector_norm`（返回
`_NormPartial`，`full_tensor()` 是跨 mesh 求和），但 `nn.utils` 用 `torch.stack`
把各参数的结果堆成一个整体再取范数，而这个 stack 不认 DTensor，**静默退化成各分片
自己的值**。实测 dp=2、三层的模型：全局范数 0.678，rank 0 报 0.551、rank 1 报
0.395。两个后果都不报错：每个 rank 按**不同**系数裁剪，以及该裁的时候不裁。

修正为 `parallel.fsdp.clip_grad_norm_` 自己归约（与 `torch.nn.utils` 在普通张量上
逐位一致，已对 `norm_type=2`、`inf`、以及裁/不裁三种情形比对），并加了一条守卫：
输入是 DTensor 而范数返回值不是 DTensor 时**抛错**，把这个静默退化变成显式失败。
反向对照：换回 `nn.utils`，测试立刻以实测数字变红
（`0.5510668158531189 != 0.6779764294624329`）。

顺带删掉 `PipelineStage.clip_grad_norm_`：**零调用方**，且它的 fallback 正是上面
那条错路径——留着只会成为第二条会漂移的实现（审计报告 §9.3.1 对「公共方法存在但
没有调用方」的定义）。

**顺带修正的两处**：`wrap_fsdp` 不再把「无 mesh 的单进程」交给 FSDP2 自己推断
（那会走 env:// rendezvous，`world_size==1` 时报 `environment variable RANK expected`，
既没提 FSDP 也没提进程组）；`examples/fsdp_train`、`scripts/parallel_matrix.py` 两处
`full_state_dict_context` 调用点改用 `get_model_state_dict`。

**验收**：206 passed（+3 条新多卡 case）；`aitrainer.__all__` 142 → 143
（减 `FSDPWrapper`/`fsdp_available`，加 `clip_grad_norm_`/`no_gradient_sync`/`supports_gradient_sync`）；
`ruff` 91（未新增，逐文件与 `HEAD` 对比过）；
3 契约 kept；`parallel_matrix --config fsdp` 2 rank `parameter_max_abs_error = 0.0`；
`examples/fsdp_train` 2 rank 跑通。

**尚未开始**（见 `~/.claude/plans` 的迁移方案）：Stage B（自研 TP/SP → DTensor）、
Stage C（统一分片保存）。**Stage D（PP → `torch.distributed.pipelining`）被阻塞**：
原生 PP 在本机 2 rank 下触发 gloo 传输层硬崩溃
（`gloo::EnforceNotMet: op.nread == op.preamble.nbytes`，`pair.cc:253`），
尚未定位根因；现有自研 PP 在 2 rank 下与单进程逐位相同，因此保持不动。

---

## DTensor 迁移（2026-09-16，Stage B 已完成）

Stage A 之后，`parallel/tp.py` 与 `parallel/sp_sequence.py` 整体换成 torch 的
DTensor 原语。**动机不是风格，是 Stage C 的前提**：旧的自研 TP 把权重换成
「分片形状的普通 `Parameter`」，没有任何地方记录它是更大逻辑张量的一片，所以
`get_model_state_dict` 返回局部张量、DCP 把同名 key 当同一个逻辑张量、最后写入者胜出。
只有参数变成 `DTensor` 才能修好。

**删除**：`ColumnParallelLinear`、`RowParallelLinear`、`SequenceParallelLayerNorm`、
`gather_sequence`/`scatter_sequence`、`sequence_parallel_dropout`、
`parallel/collectives.py` 里 7 个自研 autograd 集合通信
（`copy_to_tp`/`scatter_to_tp`/`gather_from_tp`/`reduce_from_tp`/`*_sequence`/
`all_reduce_gradient`）、`PipelineStage.clip_grad_norm_`。`all_to_all_layout` 是
Ulysses 唯一还需要的东西，移进 `sp_ulysses.py`，`collectives.py` 随之删除。

**替换**：`parallelize_module` + `ColwiseParallel`/`RowwiseParallel`；
`plugins/transformer.py` 的 `TransformerTPPlan.styles()` 产出 torch style 对象。

**本轮实测出来的四件事**（每一条都不报错，或报错报得牛头不对马嘴）：

1. **空 plan 不是 no-op。** `parallelize_module(m, mesh, {})` 成功返回、什么都不切。
   实测 `tp_size=2` 配普通 `nn.Sequential`：参数数不变、无任何提示，每个 rank 拿完整
   副本各训各的。现在 `tp_size>1` 且 plan 为空**直接报错**。
2. **torch 的 `SequenceParallel` 不能这么用。** 它假定输入**已经是本 rank 的序列切片**，
   只做标注（`from_local(..., run_check=False)`）。喂它复制张量不会切分，只会把全局
   序列长度标成 `world_size` 倍——实测 (2,4,8) 的输入返回全局 (2,8,8) 的 DTensor，
   一路无事，直到下一个模块的 `view` 抛出
   `Sharding propagation failed ... args_schema=Spec(S(1) on (2, 8, 8))`，
   既没提 SP 也没提真正的原因。它只在「embedding 就吐序列分片、整条残差流分片」的模型上
   成立，那是模型的性质，框架无法给任意 `nn.Module` 装上。改成自研 style：
   输入侧 `Replicate → Shard`、输出侧 gather 回来。
3. **手写 `chunk` 取本地切片是静默错的。** 第一版 style 就是这么写的：chunk 的反向
   只给每个 rank 自己那一片的贡献，于是*复制*输入的梯度每 rank 只填了一半——实测
   输入梯度误差 `4.8e-03` / `7.0e-03`，正确值是 `9.3e-10`。写成布局迁移后 DTensor 的
   autograd 自动给出对应的 all-gather（即旧代码里 `gather_sequence` 手工做的那件事）。
4. **TP 参数变成 DTensor 之后，FSDP 就装不上了。** `fully_shard` 要求 DP mesh 与 TP
   mesh 共享父 mesh，而 `wrap_fsdp` 一直用 `DeviceMesh.from_group(dp_group)` 造独立 mesh：
   `tp_size=2 + fsdp.enabled` 在构造期直接失败（`FSDP requires the DP and model parallel
   TP/EP mesh to have the same parent mesh`），`dp_size=2` 单独跑却没事。修法是优先取
   `mesh.submesh("dp")`（同一父 mesh 的子 mesh）。**这个问题在 Stage B 之前不可能出现**
   ——那时没有 TP 的 DTensor 参数需要 FSDP 去对账。

**顺带修掉一个从未被执行过的脚本缺陷**：`scripts/parallel_matrix.py` 的 PP 分支
调用 `result.get("loss")`，而 `ScheduleOutput` 是 dataclass 不是 dict——该脚本的 PP 条目
在 world=4 下**一个 step 都没跑完过**。删除 SP 后首轮 4 rank 运行才暴露。

**反向对照（4 条，全部确认变红）**：
- 去掉空 plan 守卫 → 「静默复制」测试红（`assert False is True`）
- `prepare_input` 换回手写 chunk → SP 输入梯度红（`0.005377 < 1e-06` 失败）
- `prepare_input` 改成完全不切分 → **数值断言全过**，只有「norm 里的激活必须是
  `Shard(1)` 且本地序列长度为全局的 1/world」这条结构性断言红
  （`assert 'Shard' in '(Replicate(),)'`）。这条证明了结构断言不可省：一个什么都不切的
  style 会与 dense 参考逐位一致。
- DP mesh 换回独立 mesh → `dp=2,tp=2` 与 `tp=2+sp` 两条组合红（父 mesh 断言）

**验收**：213 passed（+4 条组合参数化：`dp=2` / `tp=2` / `tp=2+sp` / world4 的 `dp=2,tp=2`）；
`aitrainer.__all__` 143 → 136；`ruff` 90（比基线 91 少 1，删掉的两个模块带走了既有条目）；
3 契约 kept；7/7 示例；`parallel_matrix` 4 rank 四个组合条目全部 `passed`（此前 PP 两条从未跑通）。

**明确未做**：完整 Megatron 式 SP（整条残差流序列分片）需要模型配合，框架无法通用安装，
因此**只做 norm 自身的激活分片**，与旧实现语义相同；这一限制写在 `TransformerTPPlan.styles`
的 docstring 里。`sp_backend` 的 `ulysses` 与 `megatron` 在生产路径上**行为相同**——这是
「声明了但未接线」，已记入 `架构.md`。


---

## 分片保存统一（2026-09-16，Stage C 已完成）

Stage B 把 TP 参数变成 `DTensor` 之后，这一阶段才可能：删掉
`_reject_unsupported_sharding`，让 `save_sharded`/`load_sharded` 覆盖全部轴，
并拿到此前完全缺失的**重分片**能力。

**模板句式的结果**：tp=2 存的 checkpoint，在一个**没有进程组**的单进程里读进
dense 模型，`max|diff| = 0.000e+00`，形状是全局 `(8, 8)`。这同时换了 world size
和 TP 布局。旧文档记的是「换 world size 只能拒绝，不能转换」。

**PP 没有被 TP 的修复带上，而且它坏得更隐蔽。** Stage B 之后我实测了
pp=2 的往返：`max|diff| = 6.4e-01`，两个 rank 拿到的是**同一个**张量、且都
不是自己的——而 save 和 load 都报成功。根因不是 DTensor 能解决的：
`split_sequential` 给每个 stage 各自的 `nn.Sequential`，于是每个 stage 都从
`"0"` 开始命名，stage 0 的 `0.weight` 和 stage 1 的 `0.weight` 是不同张量同一个 key。
**只**把 key 按 stage 加前缀，往返立刻变成 `0.000e+00`——反证见下。

**修复**：`checkpointing._stage_prefix`。前缀只在 `pp_size>1` 时加，这是 key
唯一性的要求而非配置开关（单 stage 时 `0.weight` 本就唯一，布局不变）。

**反向对照**：把 `_stage_prefix` 改成恒返回空串 →
`test_sharded_checkpoint_round_trips_every_axis[pp]` 红（「the pp round trip changed
the weights」），而 `[tp]` 与重分片那条**仍然绿**。这条对照同时证明了两件事：
TP 是 Stage B 修好的（与前缀无关），PP 才是前缀在修。

**本轮踩到的两个坑（记录以免重犯）**：
- `get_model_state_dict(..., StateDictOptions(full_state_dict=True))` 是**集合通信**。
  我在探针里只让 rank 0 调用，rank 0 就永远等一个不会加入的集合通信，表现是
  **整体挂死**而不是报错。同样的道理写在 `parallel.fsdp.clip_grad_norm_` 的注释里。
- `PipelineStage` 有 `parameters()` 但没有 `named_parameters()`；PP 的 worker 用例
  一开始直接调后者，`AttributeError`。

**验收**：215 passed（+2 条参数化用例，其中 PP 那条带 6.4e-01 的反证）；
`ruff` 90（未新增）；3 契约 kept；新增覆盖：TP/PP 往返、重分片到单进程。

**Stage D（PP → `torch.distributed.pipelining`）仍未开始**，阻塞条件不变
（本机 2 rank 下原生 PP 触发 gloo 传输层硬崩溃，根因未定位）。本阶段的 PP 修复
与它无关：现有自研 PP 是工作的，只是它的 key 命名需要命名空间。

---

## Stage D 阻塞项的根因（2026-09-16，已定位，未实施迁移）

方案里 Stage D（PP 换成 `torch.distributed.pipelining`）被阻塞在一个未诊断的
硬崩溃上。**根因已定位**，解阻条件满足，但迁移本身**没有做**——理由见本节末。

### 症状与最小复现

`PipelineStage` + `ScheduleGPipe` 构建正常，`sched.step()` 时两个 rank 同时
`libc++abi: terminating`，无 Python 异常。最小复现：2 rank、Gloo/CPU、一层
Linear、`n_microbatches=1/2/4` **全部**复现（所以与 microbatch 数无关）。

### 根因：torch 自己的握手张量 dtype 不匹配

定位方法：把 `_get_init_p2p_neighbors_ops`（`pipelining/stage.py:938`）里的两个张量
原样拿出来，手工做一次同样的 P2P 交换。它构造的是：

```python
recv_tensor = torch.zeros(1, device=self.device)              # float32，4 字节
send_tensor = torch.tensor(self.stage_index, device=self.device)  # int64，  8 字节
```

一发一收按**元素个数**看都是 1，按**字节**看是 8 对 4。Gloo 的 UV 传输层按字节
校验（`op.nread == op.preamble.nbytes`），于是直接 abort 进程。

对照实验（同样两个 rank、同样一次 `batch_isend_irecv`）：

| 交换内容 | 结果 |
|---|---|
| int64 发 / float32 收（torch 现在构造的） | **同样的硬崩溃** |
| 两端 dtype 一致 | **正常完成** |

### 这个握手是「哑」的，所以修它是安全的

该方法由 [pytorch#160210](https://github.com/pytorch/pytorch/commit/e63c2b21c186a7d2ab8a8953b8aa1535f2e96e58)
引入，动机是 **NCCL 多机 hang**：提前把 batched P2P 需要的通信器建好。它传的张量
内容**无所谓**，起作用的是「第一次 batched P2P 走全局 group」这件事。NCCL 那边
只看元素个数（两边都是 1），所以这个 dtype 不匹配一直没被发现；Gloo 的 UV 传输
按字节校验，就炸了。

把两个张量改成同一种 dtype（如都 `torch.zeros(1)`）后，**PP=2 端到端跑通，且梯度与
单进程参考逐位相同（`max|diff| = 0.000e+00`）**，loss 也正常。

### 为什么仍然不做这次迁移

解阻条件说的是「解阻后再评估」，评估结果是**不做**：

1. **修复必须靠 monkeypatch torch 私有方法。** 一旦 torch 改了这个方法名，补丁会
   **静默变成空操作**，崩溃以「进程直接 abort」的形式回来。为一个未修复的上游 bug
   长期背这个包袱，需要有明确收益来换。
2. **在本机可验证的收益是零。** 现有自研 PP 与单进程逐位相同（`0.000e+00`），
   打补丁的原生 PP 也是 `0.000e+00`。两者在能测到的范围内没有差别。
3. **真正值得换的功能（zero-bubble / interleaved）在本机无法验证**——需要 2 个以上
   stage 和 CUDA/NCCL。
4. **替换面很大**：`pp_schedule.py`(425) + `pp_p2p.py`(257) + `PipelineStage` 的生产
   `pipeline_step` + 17 条协议测试 + 4 条多卡用例 + `parallel_matrix` + 示例。

**结论**：记录根因与补丁配方，等有 CUDA/NCCL 机器、或上游修好之后再做。届时这是一件
已知工作量的事，而不是一次考古。

---

## DP+TP+PP+SP 全轴训练与分片存取（2026-09-16）

目标是把四个轴同时打开，训练与分片 checkpoint 都正确。做下来有两处修复，
**其中一处的影响面远超本目标**。

### 一、`pp` 与 `tp` 无法组合：`split_sequential` 重编号

`split_sequential` 用 `nn.Sequential(*layers[...])` 建 stage，按位置把子模块重命名成
`0, 1, …`。实测 stage 的 FQN 是 `['0', '1']`，于是按后缀匹配的 TP plan 一个都找不到。

改成 `PipelineSequence`（保留原名）后：

- TP plan 能在 stage 上匹配到 `q_proj`/`o_proj` → `tp=2,pp=2` 的通路打开；
- 各 stage 的 checkpoint key 不再撞名 → `checkpointing._stage_prefix` 这个绕行**删除**，
  且删掉后 PP 往返仍是 `0.000e+00`（证明名字唯一本身就够，不是靠前缀兜底）。

**反向对照**：把 `nn.Sequential` 换回去 → 「stage children are named by position again:
`['0.o_proj.bias', ...]`」，7 个组合变红（含四轴全开）。

### 二、`Runtime` 按 rank 偏移 seed：各 rank 建出**不同的模型**

这是本次最重要的发现，且**与 PP/TP 无关**——它影响所有多卡用法。

`runtime.py` 曾经是 `seed_everything(seed + self.state.rank)`。它 seed 的是**全局 RNG**，
而调用方正是从全局 RNG 取模型初始化。本仓所有示例、以及 `Trainer.__init__` 自己的
docstring 都推荐「先建 `Runtime` 再建模型」——那正是会踩中的用法。

实测（world=2）：

| 证据 | 值 |
|---|---|
| `torch.initial_seed()` rank0 / rank1 | `1234` / `1235` |
| 同一个模型，**训练前** | `max\|dW\| = 6.9e-01` |
| `dp=2` 训练后 vs 单进程参考 | `6.34e-01` |
| `pp=2` 训练后 vs 单进程参考 | `6.91e-01` |
| **`tp=2` 训练后 vs 单进程参考** | **`2.98e-08`（掩盖住了）** |

两件事把它藏住了：**TP** 的 `distribute_tensor` 从 `src_data_rank` 广播参数，直接覆盖差异；
**FSDP** 的 all-gather 把两个 rank 各一半拼成一个「看起来正常」的模型。**PP** 两样都没有，
所以是它暴露的。`tp=2` 在反向对照里**仍然通过**，正是这个掩盖的直接证据。

数据分片本来就由调用方负责（`seed_everything` 的 docstring 一直这么写：「callers remain
responsible for data samplers」），所以这个偏移没有换来任何东西。改为 `seed_everything(seed)`：
rank 0 行为不变，单进程完全不受影响。

**反向对照**：改回 `seed + rank` → 7 个变红（`dp`/`pp`/四轴全开），`tp=2` 依旧绿。

### 三、`pp` + `tp` 的结构约束（不是缺陷，是必须遵守的写法）

每个 stage 内部必须有完整的 col→row 配对。column 输出分片激活，跨 PP 边界传的是完整
张量，对端的 row 会把它当成分片。实测把 column 与它的 row 分到不同 stage 时 `fit`
**死锁**（Gloo recv 超时），不报形状错。每个 top-level child 就是一个 norm+col+row 单元
时四轴全开成立。

### 验收

`dp=2,tp=2,pp=2`（world=8）与再加 `sp=megatron`：
- **训练**后与单进程参考 `max|diff| = 2.98e-08`
- **分片 checkpoint 往返** `max|dW| = 0.0e+00`

236 passed；ruff 90（未新增）；3 契约 kept。

### 一处自身的失误，记录以免重犯

用「整段替换」（`s[:start] + new`）改 `test_multirank_gloo.py` 时丢掉了一个已有测试
（`test_sharded_checkpoint_reshards_to_a_single_process`），**当时没注意到**——它既没进
git，也没被测试数下降暴露（新加的用例把总数补上了）。本轮的教训：整段替换后必须核对
测试集合，不能只看 passed 数。


---

## 预训练权重加载：读取行为实测（2026-09-16）

用户问的是「加载预训练模型时，分片是否支持每 rank 从 CPU 直读自己的分片，而不是
rank0 完整加载再广播」。**答案是：三条路径都不是 rank0 广播**，实测如下。

| 路径 | 每 rank 读什么 | rank0 中转 |
|---|---|---|
| `Trainer.load_sharded`（DCP） | 只读自己需要的区间 | 无 |
| `convert` + `load_for_rank` | 只读自己的分片文件 | 无（docstring 明写 no rank-0 broadcast） |
| `Trainer.load_checkpoint`（非分片） | 自己那份**完整** `rank_state.pt` | 无，但 **IO 放大 N 倍** |

**证据**（tp=2、`Linear(64,64)`）：`load_sharded` 的读取项 offset——

| 参数 | rank 0 | rank 1 |
|---|---|---|
| `q_proj.weight`（列并行） | `(0,0)` | `(32,0)` |
| `q_proj.bias` | `(0,)` | `(32,)` |
| `o_proj.weight`（行并行） | `(0,0)` | `(0,32)` |
| `o_proj.bias`（复制） | `(0,)` | `(0,)` |

DCP 的读取是**分摊**的（`DefaultLoadPlanner.create_global_plan` 汇总各 rank 需求再分配），
所以别的拓扑下分配未必一一对应，但**没有 rank 读全部**。

### 本轮补上的：`stage_assignment` 自动派生

`convert` 在 `pp_size>1` 时要调用方给 `stage_assignment`，否则拒绝（理由正确：dense
state dict 看不出层属于哪个 stage）。但框架自己知道——它训练时就做这个切分。
新增 `parallel.pp_shapes.stage_assignment(model, pp_size)`，跑**同一个**
`split_sequential` 得出映射，checkpoint 与训练不会分歧。`convert` 的报错文案也指向它。

它能成立的前提是 `PipelineSequence` 保留原名；用旧的按位置重编号，每个 stage 都报
`0.weight`，派生出来的映射会是一堆同名的 `0.weight`——**而且不会报错**。

验收：单进程两条（映射内容 + 端到端 convert），另有一条多阶段的加载实测
（`pp_rank=0/1` 各自只读自己的分片，`bytes_read` 各 288，数值 `0.0e+00`）。

### 下一步（用户提出、已实测可行）

用户提出：**先构造 FSDP，每个 rank 再加载自己那一片**，这样 DP 轴也不必复制。
实测成立——在已 `fully_shard` 的模型上，每 rank 按 `(pp,tp,dp)` 取自己那一段、
经 `param.to_local().copy_(local)` 写进本地 storage（需 `no_grad`），
`full_tensor()` 能**精确重建**完整权重（`max|diff| = 0.000e+00`）。

这把 DP 从「复制」变成「切分」，是路线图 §「模型初始化」一节已经写明的方向
（「再传输到本地参数、DTensor 或 FSDP storage」）。尚未接线。

---

## 预训练权重按拓扑分片加载（2026-09-16）

用户的目标：训练开始时加载预训练模型，**每个 rank 从 CPU 直读自己那一片**，
而不是 rank0 完整加载再广播。已交付。

```python
plan = TransformerTPPlan()
CheckpointConverter(partition_dims=plan.partition_dims(model)).convert(
    "pytorch_model.pt", "sharded/", dp_size=2, tp_size=2, pp_size=2,
    dp_sharded=True, stage_assignment=stage_assignment(model, 2))
trainer.load_pretrained("sharded/manifest.json")
```

实测每 rank 读取量随拓扑按 `1/(dp·tp·pp)` 下降（完整模型 2304 字节）：

| 组合 | 每 rank | 比例 |
|---|---|---|
| `dp=2` / `tp=2` / `pp=2` | 1152 / 1216 / 1152 | ~1/2 |
| `dp=2,tp=2` / `tp=2,pp=2` / `dp=2,pp=2` | 608 / 608 / 576 | ~1/4 |
| `dp=2,tp=2,pp=2` | **304** | **1/8** |

三个前提，每个都有实测支撑：

1. **DP 参与分片，但只在 FSDP 时成立**，且 manifest 与加载方必须一致——不一致就报错。
   DP 是复制时每 rank 需要完整参数、文件里却是切片，反过来同理，两种都会**静默**给出
   残缺模型。manifest 新增 `dp_sharded` 字段承载这个声明。
2. **`partition_dims` 决定 IO 省不省**。不给它，convert 把所有张量记成 replicated
   （实测：每 rank `bytes_read` = 完整模型的 2304），加载正确但什么都没省。
   `TransformerTPPlan().partition_dims(model)` 镜像 torch style 的真实行为派生它。
3. **布局不一致时报错，不猜**。`_assign` 按形状区分「这是我本地那片」（直接写）与
   「这是完整张量」（DTensor redistribute），都不是就报错。

**反向对照**：把 `partition_dims` 去掉 → `rank 0 read 2304 bytes for a 2304-byte model
-- it read everything` 变红；`dp=2,tp=2` 则是形状不符的显式拒绝（说明没有静默切错）。
4 红 3 绿，只有需要切分的组合变红。

### 顺带修掉一个静默丢字段的缺陷

`Manifest.from_dict` 逐字段取值，`dp_sharded` 加进了 dataclass 和 `to_dict`
（用 `asdict`）却漏在 `from_dict` 里：manifest **写进了 JSON**，读回来是默认的
`False`，加载方于是报「converted with dp_sharded=False」——字段就在文件里。
新增 `test_manifest_round_trip_keeps_every_field` 用 `dataclasses.fields()` 驱动往返，
以后再加字段忘了序列化会直接变红。

### 一处自身的失误

`reader.py` 里连做两次 `write_text`，第二次用了**旧内容**，把第一次的结果覆盖掉了
（表现为 `_assign() got an unexpected keyword argument`）。整段替换的编辑方式在这个
会话里已出过两次问题（另一次是丢掉一个测试）。教训同上：改完要跑，不能只看编辑成功。

**验收**：246 passed（+7 条按拓扑参数化的加载用例）；ruff 未新增；3 契约 kept；7 示例通过。

---

## SP 下复制参数的梯度从未归约，optimizer state 因此是错的（2026-09-17，已修）

发现路径本身值得记：它由**分片 checkpoint 的一致性检查**报出来，但根因与 checkpoint 无关。

### 现象

`tp2_sp`（`tp_size=2, sp_backend=megatron`，`dp_size=1`）的分片 checkpoint：

| 检查 | 结果 |
|---|---|
| 存档往返（参数本身） | **通过**（逐位相同）|
| 续训一致 | **失败**，相对差 `2.08e-01` |

参数是对的，**optimizer state 是错的** —— 所以 load 看起来完全成功。

### 定位

逐个参数比对 momentum buffer：**64 个里错 16 个，且全部是 `*norm*` 参数**。对照矩阵把范围锁死：

| 组合 | TP | SP | FSDP | 结果 |
|---|---|---|---|---|
| `tp2` | ✓ | ✗ | ✗ | 通过 |
| `dp2_tp2_sp` | ✓ | ✓ | **✓** | 通过 |
| **`tp2_sp`** | ✓ | ✓ | ✗ | **失败** |

**FSDP 只是把缺陷掩盖了**：它给 norm 参数再包一层 shard，reduce-scatter 顺手把 TP 轴的
偏和归约掉了（实测 `dp2_tp2_sp` 里梯度是 `(Shard(0), Replicate())`，没有 `Partial`）。

### 根因

`parallel/tp.py` 的 SP 样式把 norm 参数做成 `Replicate` DTensor，并把每个 rank 的
**序列切片**喂给 norm。于是参数梯度是**每个 rank 各一份的偏和**，而
**autograd 从不归约它**：

```
0.norm1.weight      param=(Replicate(),)  grad=(Partial(sum),)   local=[0.017554, 0.001037] (rank0)
                                                                 local=[0.003271, 0.006034] (rank1)
0.attn.o_proj.bias  param=(Replicate(),)  grad=(Replicate(),)    ← 同一个模型里，由 Replicate 激活喂的
```

**代码里原本的 docstring 断言了相反的事**（"DTensor propagates a Partial gradient ... and
reduces it in autograd"）—— 实测只传播、不归约。三个后果，安静的才危险：

* **参数更新是对的**，因为 DTensor 在计算新参数值时会归约 —— 所以 loss 曲线和每一步后的权重都看着正常；
* **optimizer state 是错的**：SGD 的 momentum buffer 变成 `(Partial(sum),)`，每个 rank 存自己的偏和。
  分片 checkpoint 于是把某一个 rank 的偏和写给所有 rank，续训因此分岔；
* **任何直接读 `.grad` 的东西都是错的**，梯度裁剪首当其冲 —— 它会裁一个偏和的范数。

### 第一次修法是静默的空操作

第一版在 SP 样式里注册 `register_post_accumulate_grad_hook`。它**能工作**：实测梯度变成
`(Replicate(),)` 且跨 rank 一致，多卡测试也通过。

**但 `model.to(device)` 一次调用就会让它永久失效。** `nn.Module.to` 重新指向 `.data`，hook 仍列在
参数上、`.grad` 照常被填 —— 填的是未归约的 `Partial` —— 而不抛任何异常。实测：

```
1 no .to()          : fired=1 grad=(Replicate(),)
2 after .to(cpu)    : fired=0 grad=(Partial(sum),)
```

而 `Trainer.__init__` 对每个模型都调用 `.to()`。所以这个修法在框架自己的路径上是**死的**，
只在孤立探针里看着对。

### 修法

改成在**消费梯度的地方**归约：`trainer/step.py` 的 `run_optimizer_step`，在梯度裁剪与
`optimizer.step()` **之前**调用 `parallel.tp.reduce_replicated_gradients`。它只碰两个条件同时成立的参数
（**参数**全 `Replicate` 且**梯度**全 `Partial`），其余原样不动，所以对没有 Partial 梯度的模型是空操作。

顺带更好：从"每个 microbatch 一次"降到"每个 optimizer step 一次"。

### 反向对照

把 `run_optimizer_step` 里的那一行换成 `pass`，`test_sequence_parallel_matches_dense` 变红，
且是专门的诊断：

```
AssertionError: the optimizer's momentum buffer is not replicated, so each rank
stores a different partial sum; the update still looks right, but gradient
clipping and every checkpoint of this state are wrong.  Got '(Partial(sum),)'
```

### 为什么现有测试没抓到

该测试的数值列全部走 `full_tensor()`，而**它对 `Partial` 会做 all-reduce** ——
一个从未归约的梯度因此**等于** dense 参考，整条断言照常通过。检查方法本身掩盖了缺陷。
现在补了两条看得见它的断言：裸 backward 的梯度**必须是** `Partial`（钉住成因），
而 optimizer 的 momentum **必须是** `Replicate` 且跨 rank 一致（钉住结果）。

## 验证脚本补上分片 checkpoint，以及跨策略权重一致性（2026-09-16）

`scripts/verify_parallel.py` 现在每个组合都额外做两件与 checkpoint 有关的事，并给出
**跨策略**的权重对照。

### 新增的两项检查

**存档往返**：`save_sharded` → 新 trainer `load_sharded` → 参数必须**逐位相同**。
写读是无损的，所以这一项用**相等**而不是容差 —— 有差值就意味着数据写到了错误的分片、
拼错了，或者丢了。TP / FSDP / PP 各自以不同方式切分 state dict，所以它对每个组合都是
一次真实的检验。

**续训一致**：两边各再走一步，必须落到同一处。这是 checkpoint 存在的理由，而且它覆盖了
往返覆盖不到的状态：**optimizer 的动量、步数计数、调度器、以及每一路随机流**。
因为要验证"optimizer 恢复了"，脚本的优化器必须是**有状态的** —— 纯 SGD 两步之间什么
都不留，一个没恢复 optimizer 的 trainer 会走出**完全相同**的一步，续训那一列会恒为
`0.00e+00` 而什么都没测。用 SGD + momentum（它每参数带一个 buffer）。

### 找到的缺陷：`load_sharded` 成功返回，optimizer 却已经废了

**实测（两阶段流水 + SGD+momentum）**：`load_sharded` 正常返回，但 **rank 0** 的
optimizer `param_groups` 回来时**少了 `dampening` / `lr` / `momentum` / `weight_decay`**，
而 rank 1 完好。损失当场不报，**下一步** `optimizer.step()` 才炸：

```
KeyError: 'momentum'      （换成 AdamW 则是 KeyError: 'betas'）
  torch/optim/sgd.py → optimizer.step()
```

torch 报的是**键名，不是它去哪了**，而 `load_sharded` 早就返回了。

**排查过程排除的东西**（都实测过）：checkpoint 里 10 个键**都在**；
`get_optimizer_state_dict` 在 save 侧与 load 侧给出的 `param_groups` **结构完全一样**；
`cast_optimizer_state` 只动 `state` 的张量、不动 `param_groups`。所以是 **DCP 填充
recipe 时的行为**。

**顺带发现一个守卫的粒度不够**：前几轮加的 `_reject_unclaimed_namespaces` 按**命名空间**
比较（当时论证过：按叶子比较会因为各 rank 分区而误报）。而这个丢失发生在
`optimizer.param_groups.0.*` **命名空间内部**，所以守卫看不到 ✓ 它的粒度对它的目的是
对的，但覆盖不到这一层。

**修法**：`_restore_missing_group_keys`，在 `set_optimizer_state_dict` 之后把缺的键用
**载入前的值**补回（`setdefault`，只补不覆盖），并在**组数不一致时报错**而不是错位对齐。

### 跨策略权重一致性

报告新增一张**两两对照矩阵**：任意两种策略训练后的最终权重之差。这是"它们都是同一套
训练的等价实现"的直接检验，**不需要基准就能陈述**。它失败的方式也不同——两个共享同一
缺陷的组合会**互相一致**而与基准都不一致，所以两张表合起来比任一张说得都多。

**矩阵里的非零值需要解释**（实测，world 2+4 共 12 个组合）：

| 对 | 距离 |
|---|---|
| `dp2` vs `dp4`、`tp2` vs `dp2_tp2` | **恰好 0** |
| 跨类型（如 `dp2` vs `tp4`） | ~2e-07 … 3.4e-07 |

**归约顺序相同的组合逐位一致，顺序不同的差几个 ulp**：DP 的 all-reduce、TP 的 row 投影
求和、PP 的逐 microbatch 累加，把同一批数按不同次序相加。这是浮点结合律，不是算错了什么
——也正因如此这一项的判据是**容差**，而 checkpoint 往返那一项是**相等**（同一批字节回来）。

### 仍未查清的一项

`tp2_sp` 的**续训**不等价（`2.082e-01`），而它的**往返是好的**。对照：`tp2`（无 SP）通过，
`dp2_tp2_sp`（有 SP 且有 dp）也通过 —— 只有这一个组合失败。脚本如实把它标为失败，
原因待查。

### 验证

**测试**：`test_sharded_load_restores_optimizer_group_keys_it_dropped`（纯函数：补回、
不覆盖已有值、组数不符则拒绝）与 `test_pipeline_checkpoint_resume_keeps_the_optimizer_usable`
（真 2 进程、PP、momentum、save → load → **step**，因为缺陷是"load 成功而 step 炸"）。

**反向对照**：去掉 `_restore_missing_group_keys` 的调用 → 多卡测试红。

**一处自身的失误**：多卡测试最初硬编码了端口 `29877`，与别的多卡测试在全量运行时撞车，
失败看起来像是这个测试自己的问题。改为绑 0 端口取空闲端口。

**验收**：263 passed（+2）；ruff 84（基线未变）；3 契约 kept。

---

## 一键验证脚本，以及它抓到的两个真缺陷（2026-09-16）

新增 `scripts/verify_parallel.py`：不传参数就依次跑本机能起的每个 world size，
对每个并行组合与**单机单卡基准**比四项——前向输出、loss、梯度、参数更新——并输出
JSON + Markdown 报告。基准在 launcher 进程里跑（那里没有进程组），组合走
`Trainer.train_step`（**框架自己的路径**，不是手写的 forward/backward 循环）。

### 缺陷 1：`GPipeSchedule.run_middle_stage` 对「收到的激活」求梯度

```python
activations.append(activation)                  # ← 存的是本 stage 的输入（来自上游）
...
grad = self._recv_backward_any(specs[index])    # ← 这是针对 output 的梯度
_autograd_backward(activations[index], grad)    # ← 却对输入 backward
self.communicator.send_backward(activations[index].grad, ...)
```

输入的图属于**上游 stage**（在另一个进程），本进程 backward 它**什么也算不出来**。

**实测 pp=4**：

| stage | 0 | 1 | 2 | 3 |
|---|---|---|---|---|
| 修复前 max_grad | 0.199 | **0（全部 `grad is None`）** | **0（同左）** | 0.206 |
| 修复后 max_grad | 0.176 | 0.195 | 0.201 | 0.206 |

**中间 stage 的 16 个参数全部 `grad is None`，而 `backward_complete` 是 True，loss 精确到
1.8e-07** —— 不报错，只是中间层不学习。而 `activation.grad` 就是收进来的梯度**原样**，
所以上游收到的是**没乘过本 stage 雅可比**的梯度。

**为什么 pp=2 时正常**：pp=2 没有"中间 stage"，`run_middle_stage` 根本不被调用。
首 stage 与末 stage 一直是对的，**所以任何两阶段的运行都测不到它**。

**既有测试为什么没抓到**：`test_pp_schedule_protocol.py` 的 17 项断言全是"**发了什么**"——
消息顺序、stage 标签、microbatch 标签。而原样转发的梯度**形状对、非 None**，
在协议层与正确梯度**无法区分**。协议模拟器覆盖通信，不覆盖"梯度是否真的算出来"。

### 缺陷 2：`OneFOneBSchedule._run` 的同一处错误

1F1B 的 `records.append((activation, spec, loss))` 与 `backward` 里的
`_autograd_backward(activation, ...)` 是同一个错误，只是写在另一条代码路径上。
症状、量级、隐藏方式完全相同（实测 `pp4_1f1b`：grad 1.103 / param 1.000）。

**修法**（两处相同）：记录 `output` 并对**它**求梯度——那会同时填好本 stage 的参数梯度和
`activation.grad`（收到的激活是 leaf）；收梯度用 **output 的 spec**，发梯度用 **input 的 spec**。
1F1B 的 `records` 因此从 3 元组变成 5 元组（backward 目标、读 `.grad` 的张量、收/发 spec、loss）。

### 这个脚本自己犯过的三个错（记录以免重犯）

写它的过程中我的判断错了三次，每一次都是**脚本的问题被误读成框架的问题**：

1. **对 PP 用 `model(x)` + `loss.backward()`**：`PipelineStage.__call__` 只是把自己的
   module 前向一遍，**不做跨 stage 调度**。测出的 0.7 参数误差是脚本的，不是框架的。
2. **用 `sum` 概括参数变化**：权重更新常正负抵消，`sum(Δ) ≈ 0` 被读成"没更新"，
   实际是**度量本身没有分辨力**。换成 `max|Δ|` 后 49/64 的"不符"里有一半消失。
3. **`cases_for(4)` 里包含 world-2 的组合**，而运行时只起 4 个 rank → 报
   "a rank produced nothing"，一个列表构造错误穿成了框架失败。已加自检：轴的乘积必须
   等于 `world_size`，且 `pp` 不能超过模型层数。

### 验证

**测试**：`test_every_pipeline_stage_receives_gradients`，参数化覆盖 **gpipe 与 1f1b 两种调度**
（它们经不同的代码到达中间 stage），world=4，断言每个 stage 的 `without_gradient == 0`
且 `max_grad > 0`，并断言只有末 stage 报出 loss。

**反向对照**：把 `run_middle_stage` 的修复退回去 → 红；把 `_run` 的修复退回去 →
**只有 1f1b 那条红**（1 passed, 1 failed），证明参数化确实分辨了两条路径。

**端到端**：`verify_parallel.py` 在 world 2/4/8 上共 20 个组合（含 `dp2_tp2_pp2_sp`
与 `dp2_tp2_pp2_1f1b`）全部与单进程基准一致，四项误差 ≤ 1e-5。

**验收**：261 passed；ruff 84（基线未变）；3 契约 kept；7 示例通过。

---

## 分片 checkpoint 缺 CUDA 随机流，以及 GPU 验证清单（2026-09-16）

整理「本机无法验证的断言」清单时，发现**前几轮的 RNG 修复是不完整的**。

`checkpoint/manager.py` 的 per-rank 格式一直存了 CUDA 生成器：

```python
return {"torch": torch.get_rng_state(),
        "cuda": torch.cuda.get_rng_state_all() if torch.cuda.is_available() else None}
```

而我在前几轮给分片格式补 `_rng_state()` 时，只补了 **CPU torch + Python** 两路，**漏了 CUDA**。
后果：在 GPU 上，从分片 checkpoint 续训的 run 会与写出它的 run **随机流不同**——GPU 上的
dropout、随机增强全都分岔，没有任何提示。本机 `is_available()` 恒为 False，
`_rng_state()` 因此**根本不加 `"cuda"` 键**，所以这条缺口在原环境里**结构上不可见**。

已补齐（镜像 per-rank 的做法，`torch.stack` 成 `(devices, bytes)` 以保持 DCP 需要的固定形状），
并加了一条**结构测试**：用假的 cuda 接口断言"存时取了 `get_rng_state_all`、恢复时调了
`set_rng_state_all`"。本机只能验到这里——数值只能在 GPU 上验，见下。

**同时修掉的第二个盲点**：`scripts/verify_random_state.py` 的 `checkpoint-rng` 检查原先
用 `torch.randn(3)`，即**CPU** 上的生成器。所以即使在 GPU 上跑那个审计，它**也测不到 CUDA 流**——
一个专门用来抓随机流问题的工具，对最可能出问题的那条流是盲的。已改为在**训练设备**上取。

### GPU 验证清单

新增 `docs/GPU验证清单.md`：把代码里所有 `torch.cuda.is_available()` / `device.type == "cuda"`
的分支逐条列出（12 处），每处就是一条"只在 GPU 上走"的路径，再按**失败后果**分成三类——
A 类会算错而不报错、B 类会报错、C 类只是"有没有用"。每项给出：断言、本机为何测不到、
怎么测、**预期结果**、失败意味着什么。

其中最值得先跑的两条：

- **A1 CUDA 随机流**（上面那条）
- **A2 GradScaler**：`torch.amp.GradScaler("cuda", enabled=True)` 在无 CUDA 的机器上被 torch
  **自动降级**为 `_enabled=False`，于是 `state_dict()` **恒为 `{}`**（实测）。所以两种格式的
  scaler 分支在本机都是"写空、读空"，测试通过但**什么都没验证**；GPU + fp16 时它返回 5 个字段，
  那条路径**才第一次真正执行**。

清单里也记下了 `tests/` 中 `cuda.is_available` **零出现**这个事实：测试套件没有任何 GPU 相关
的 skip，意味着上述每条分支在 GPU 上都会走，而它们都没有被专门测过。

**验收**：258 passed（+1 条 CUDA RNG 结构测试）；ruff 84；3 契约 kept。

---

## 三项收尾：删掉没有接点的抽象（2026-09-16）

按既定排序的第 3–5 项。用同一个判据筛：**这个机制在框架允许的配置空间里，有没有接点？**

我上一条对第 5 项的推荐是「接线」，**那个判断错了**——它基于不完整的调查。核实后的结论是删。

### `GradientBucketReducer`（75 行）—— 配置空间数学上排除

```
capability.py:150   dp_size > 1              →  必须 fsdp.enabled=True
gradient.py:38      GradientBucketReducer(fsdp=True)  →  raise
```

`dp_size>1` 时它被拒；`dp_size==1` 时没有 DP 归约可做。**框架允许的组合里不存在能让它有用的一组。**
它是 DDP 的 gradient bucketing 方案，而本仓 `dp_size>1` 强制走 FSDP2，其 reducer 自带分桶。

连带删除的**知识**（代码没了，记录留下）：`finish_grad_sync` 曾在 `reduce_fn=None` 时
清空桶并把所有已注册的梯度**静默丢掉**；原实现用一次性 `_finished` 闩锁让它在进程生命周期内
只归约一次、之后每个窗口都静默跳过。

### `MicrobatchInterleaveScheduler`（65 行）—— 接点已被删除

它期望 `forward_fn` 返回 `(activation, AsyncOp)`：

```python
if isinstance(output, tuple) and len(output) == 2 and isinstance(output[1], AsyncOp):
    activations[-1], handle = output; handles.append(self.scheduler.register(handle))
```

唯一能产生那个形状的是**旧的自研 TP**：

```python
output = self.overlap_controller.submit("tp.backward_dgrad_reduce", lambda: reduce_from_tp(...))
```

`git log -S` 显示它在 `773107d`（DTensor 迁移）中被删除，当前 `src/` 里 `overlap_controller`
**零出现**。所以它**不是"未接线"，而是线的那一头已经被删掉了**。它的 2 条测试能过，是因为
测试 mock 了那个返回形状——测试活在一个假世界里。

### `kernels/backend.py` 的选择抽象（约 120 行 + 4 条能力声明）—— 与 PyTorch 重复

**每一项宣称的价值都被 PyTorch 覆盖，或两端皆空**：

| 它宣称提供 | 实际情况 |
|---|---|
| 按可用性挑优化 kernel | **PyTorch dispatcher 已经在做**。实测 `fused.layer_norm` 就是 `F.layer_norm` 的薄包装（逐位相同），而 `nn.LayerNorm` 底层也调它 → 接线前后**既无行为差异也无性能差异**。CUDA 上 `F.layer_norm` 就是 fused kernel |
| 接入第三方后端 | **两端都空**：全仓**零处** `import flash_attn` / `transformer_engine` / `triton`；`installed_optional_backends()` 只被 `diagnostics.py:70` 拿去**报告** |
| 记录实际用了哪个实现 | 唯一真实价值，但 `used_fallback` **无人读**——而给它造一个消费方就是"为模块找用途" |
| attention 的 dispatch | `scaled_dot_product_attention` **自己**就做了探测+回退（`use_sdpa` + `_eager_attention`），`attention_capability()` 的 docstring 自己写着"是描述，不是注册令牌" |

`fused.py` 的算子也不稀缺：**torch 2.9.1 有 `nn.RMSNorm` 和 `F.rms_norm`**。

**保留**：`kernels/fused.py`（5 个算子）与 `attention.py` 的 `scaled_dot_product_attention` /
`flash_attention_available`，定位为**参考实现 + 数值基准**——`rms_norm` 的注释记录了实测的
bf16 偏差（先乘再 cast 得 7e-3，先 cast 再乘更低），它的测试保留。公开导出 14 → 7。

### 顺带修掉一个悬空的 capability 引用

`compile` 的 `fallback="eager"` 指向一个**矩阵里不存在**的名字（`fp8` 的 `fallback="bf16"` 是对的）。
改为 `single_process_eager`。加了一条检查：`capability_matrix()` 的每个 `fallback` 都必须指向
真实存在的条目。这也验证了本轮删除没有留下别的悬空引用。

### 验证

**测试集合的变化逐条核对**：268 → 257（−11）。删除的：`test_kernel_backend.py` 2 条、
`test_eager_fallback_is_selected_and_not_registerable` 1 条、`test_microbatch.py` 2 条、
`test_gradient_bucket.py` 1 条、`test_async_runtime.py` 的 bucket 边界 1 条、
`test_audit_regressions.py` 的 reducer 3 条、`test_overlap_boundary.py` 的 FSDP 冲突 1 条。
**没有一条是误删的**——每一条测的都是被删的抽象本身。

**保留并仍然验证的**：`test_rms_norm_stays_within_its_error_budget`（数值主体不依赖抽象，
只有尾部两行依赖 `KernelCapability`，已去除并把预算的来源写进 docstring）。

**验收**：257 passed；ruff 84（基线未变；中途新增的两条 RUF022/F401 是行号位移与失效导入，已处理）；
3 契约 kept；`overlap_smoke` passed（4 项检查）；7 示例通过。

---

## 接线 `parameter_prefetch`：三层独立缺陷，机制修好但驱动仍缺（2026-09-16）

按既定排序的第 2 项。**它比看起来坏得多**：原以为只是"没人调用 `finalize()`"，
实际是三层独立缺陷叠在一起，每一层都足以让这个功能在**任何**环境下保持不工作。

### 缺陷 1：`finalize(expected=...)` 永远返回 False

`fingerprint` 收一个默认 `None` 的 `config` 参数，而 `finalize` 内部重新计算时**不传它**：

```python
def fingerprint(self, config=None):  payload = {"trace": [...], "config": repr(config)}
def finalize(self, expected=None):   fp = self.fingerprint()          # ← repr(None)
```

调用方用 `fingerprint(config)` 算出的指纹，与 `finalize` 重算的 `fingerprint(None)`
**永不相等**（除非调用方也传 None）。于是 `valid` 永远是 False，`prefetch_async` 永远
退化到同步分支。

**没被发现**是因为 `fingerprint` 的唯一调用方就是 `finalize`，而所有测试都调
`finalize()`（不传参）——那条路径**跳过对比**，直接返回 True。

`config` 参数本身也**从来没有任何真实调用方**，即又一个死参数；而它恰恰禁用了它被加进来
服务的那个机制。已删除：访问顺序已经蕴含在 trace 里，不影响顺序的配置变化**不该**作废
prefetch 计划。

### 缺陷 2：指纹记的是内存地址

`register_module` 记录 `str(id(p))`。id 是内存地址，所以**同一个模型两次运行的指纹不同**
（实测）。即使缺陷 1 修好，`finalize(expected=<warmup 指纹>)` 依然永远失败——机制的第二道锁。

改成参数**名**：跨运行稳定，且出现在不匹配时抛出的错误里。

### 缺陷 3：trace 只有一条记录，指纹没有信息量

`register_module` 把**整个模型**当成一条 `TraceEntry`。实测一个两层的模型只产生
`TraceEntry(module='Blk', ...)` 一条——一个只说了容器类名的指纹，对"层的顺序"一无所知，
而层的顺序正是 prefetch 计划唯一依赖的东西。

改为**逐层**递归注册（`named_children` 前序），名字**全限定**（`b0.norm` 而非 `norm`——
`named_children` 会把两个 block 里的 norm 都叫 `norm`，局部名区分不了这两个位置）。
`_module_ids` 这个此前**只写不读**的死字段在这里有了用途：挡住共享子模块被记录两次。

实测：两层的模型从 1 条变成 8 条；同结构两次运行指纹相同；结构变化指纹不同。

### 还修了一个静默洞

`prefetch_async` 在 coordinator 因**预算**拒绝时返回 `None` 而**不做任何搬运**——调用方把
"被拒绝的预取"读成"已经取好了"，参数还在主机上，没有任何异常。现在两条拒绝路径
（闸门未验证、预算不足）都**同步取一次**再返回 None。

### 还修了"根本不会重叠"

`prefetch_async` 把搬运存成 closure 塞进 `op.handle`，`op._wait_fn = lambda fn: fn()`，
所以**搬运是在消费者 `wait()` 时才执行的**——延迟取值，不是提前取值，什么都不隐藏。
现在提交时发起（`non_blocking=True`，`_copy_event()` 在 CUDA 上记录 event），
`wait()` 只做同步。

### 仍未接的：逐层驱动（这一项剩下的全部）

机制现在是对的，但**没有任何调用方**：`Trainer` 是**整模型**粒度
（`step.py` 的 `fetch(model.module)` / `release(model.module)`）。要做按层预取需要：

1. 一个按 trace 顺序驱动 fetch/release 的调度器（forward hook 或分层 forward）
2. **与 backward 达成一致**——同一批参数在反向也要用，所以 release 的时机由反向的层次
   结构决定，而不由前向决定

这是独立的设计任务，且**收益在无 GPU 时无法验证**（CPU 上 `fetch` 是空操作）。按本仓
"未验证不得标记完成"的立场，没有盲写。

### 验证

**测试**：4 条（指纹跨运行稳定、结构变化移动指纹、同构/异构子层可区分、trace 逐层而非
整模型）+ 3 条（`finalize` 能返回 True、提交时已发起、预算拒绝时 fallback）。

**反向对照 5 条**：回到延迟执行 → 红；去掉预算 fallback → 红；指纹记回 id → 红；
退回局部名 → 红；不再逐层递归 → 红（3 failed）。

**一处自身的失误**：我第一次选的反向对照是"把 `config` 参数加回 `fingerprint`"——它**没红**，
因为我的修复同时移除了调用方的 `config`，两边都用 `repr(None)` 仍然一致。那条对照**不构成
原来的 bug**。换成"让指纹不反映访问顺序"后正确变红。

**验收**：268 passed（+7）；ruff 84（中途新增的 F401 已修）；3 契约 kept；`overlap_smoke` passed。

---

## 接线 `Profiler`：`overlap_ratio` 从结构性 0 变成实测（2026-09-16）

按既定排序的第 1 项。目标环境是真实多机多卡 CUDA，所以这一项的方案选择也跟着变了。

**为什么手写打点接不上**：`record_async` / `record_wait` 需要在每次集合通信处有调用点，
而**值得度量的通信全在 DTensor / FSDP2 内部**——它们是 torch 的 autograd 节点，
本框架没有调用点可挂。这就是它一直是结构性 0 的原因。

**方案：从 `torch.profiler` 的 trace 里读。** 实测（2 rank Gloo）它**能**抓到
`FSDP::all_gather`、`gloo:all_reduce`、`c10d::_reduce_scatter_base_` 等，带时长。

### 匹配规则是这一项的核心，它不能靠猜

一次集合通信在 trace 里是**一个嵌套**，实测两个 rank：

```
FSDP::all_gather            user_annotation  dur=217   ← 包装
  fsdp::all_gather_copy_in  cpu_op           dur=26    ← 本地拷贝，不是通信
  c10d::_allgather_base_    cpu_op           dur=12    ← 发起调用（是**兄弟**不是父）
  gloo:all_gather           user_annotation  dur=118   ← 真传输，在**独立线程**上
  FSDP::all_gather_copy_out user_annotation  dur=42
```

按算子名匹配会把它算成 217+26+12+118+42。所以匹配**后端前缀**（`gloo:`/`nccl:`/…），
那是名字里唯一标识"传输本身"的部分。`c10d:` **不在**前缀表里：`c10d::_allgather_base_`
是发起调用且与传输**并列**，收了它每次通信都算两遍。

### 拼图里露出一处既有缺陷：`overlap_ratio` 上界是 0.5

`record_wait` 累加的是 `min(seconds, overlapped_seconds)`，把 hidden **clamp 到不超过**
exposed。而 `ratio = hidden/(exposed+hidden)`，所以 `ratio ≤ 0.5`——**而"通信几乎全被藏住"
正是重叠优化的目标，ratio 本该能接近 1.0。**

没被发现是因为 `record_wait` 零生产调用方：两条既有用例（`record_wait(4,4)`、
`record_wait(4,1)`）的 hidden 都恰好 ≤ exposed，从没碰到过上限。已改为独立累加，
并加了一条钉住"hidden 可以大于 exposed"的测试。

### 实测结果（2 rank，FSDP，3 步）

| rank | 抓到的通信 | exposed | hidden | overlap_ratio |
|---|---|---|---|---|
| 0 | 6 | 957us | 35us | 0.035 |
| 1 | 6 | 887us | 37us | 0.040 |

**非零、非 1**：CPU/Gloo 上通信基本暴露（调用方在等），少量被计算盖住——这是真实测量，
不是恒 0 也不是恒 1。

**本机界限（必须写清）**：CUDA 那一半**未验证**。CPU/Gloo 的后端在 worker 线程上传输，
所以区间真的会交错、split 有意义；CUDA 上 NCCL kernel 与 compute kernel 并发，
同一套算法会给出重叠优化真正想推动的那个数。本机无 GPU，只能验到这个界限为止。

**测试**：4 条合成 trace 测试精确验证算法（完全暴露/完全隐藏/部分重叠、**并发 kernel 只算一次
而不是求和**、包装层不算第二次、无通信的 trace **报错而不是返回 0**），1 条钉住 ratio 上界，
1 条真实 2-rank 端到端。

**反向对照**：把 `min()` clamp 加回去 → 2 failed；让 `capture()` 不再喂数据（回到结构性 0）
→ 多卡测试红。

**一处自身的失误**：判断"测试是否变红"时我用了 `grep -qE "1 failed"`，而实际输出是
`2 failed`——**"1 failed" 不是 "2 failed" 的子串**，于是脚本报"没红"，差点让我以为
反向对照通过。改用 `grep -oE "[0-9]+ failed"` 取数值判断。

**验收**：261 passed（+6）；ruff 84（基线未变，中途新增的 UP037 已修）；3 契约 kept。

---

## 收尾三项（2026-09-16）

三项都是「声明与实现不符」，不是行为缺陷。

**1. `capability.py` 描述一套已被删除的 API。** `pp_p2p_overlap` 的说明写着
「the preposted receive API (`post_recv_forward`/`post_recv_backward`/`wait_posted`) has no
caller」——这三个名字**在 `src/` 里只存在于这个字符串中**，代码已在 `7d73b2c`
（"stage2: delete dead API and inert configuration"）被删除。改为描述现实：异步发送那半边是
**接线的**且 1F1B 路径**无条件**发它，所以这个开关没有东西可控，只剩「`pp_size<=1` 时拒绝」；
投递式接收从未建成，接收只有同步一条路。

**2. `src/aitrainer/lifecycle.py` 与 `core/lifecycle.py` 逐字节相同**（`diff` 无输出）。
`src/` 内 10 处导入全走 `core.lifecycle`，`api.py` 也是，唯一用户是
`scripts/overlap_smoke.py:21`。已删除重复文件并把脚本指向 `core`。

它比「冗余」更糟的一点：import-linter 的 `layers` 契约**根本没有列出这个模块**，
所以它可以任意 import 任何一层而不被发现——契约存在的意义就是防这个。

**顺带**：删掉它让 ruff 从 **90 降到 84**（该文件自带 6 个基线错误）。这不是"清理既有错误"，
是删除的副产品，但它改变了基线数字。自本节起基线为 84；上面几节记录的 90 是各自提交时的真实值。

**3. 目标态文档里的残留字段。** `路线图.md` 与 `开发方案与开发规划.md` 描述的是**目标**
（README 明确这么说），所以目标里留着一个我们已判定**不该存在**的字段，等于指示未来的实现者
把它加回来。

- `路线图.md:368` 的「精度配置拆分为 `param_dtype`、`compute_dtype`、`grad_dtype`、
  `reduce_dtype` 和 `optimizer_dtype`」→ 改为说明参数存储与 FSDP 归约 dtype **都由
  `FSDPConfig.mixed_precision.dtype` 一个字段表达**（FSDP2 的 `MixedPrecisionPolicy` 就是
  从一个 dtype 同时设 `param_dtype` 和 `reduce_dtype`），两个独立字段已从目标中删除
- `开发方案与开发规划.md` 的目标态 `PrecisionConfig` 代码片段去掉 `param_dtype` 与
  `reduce_dtype`，`AdvancedOverlapConfig` 去掉 `enable_parameter_prefetch`，各附一行理由

`路线图.md:334` 的 `MixedPrecisionPolicy(param_dtype, reduce_dtype)` **保留**——那是 FSDP2
自己的 API，说法正确。

**验收**：255 passed；ruff **84**（基线，见上）；3 契约 kept；7 示例通过；
`overlap_smoke` passed；`check_no_reference_imports` 通过。

---

## `sp_backend="ulysses"` 从静默降级改为校验期拒绝（2026-09-16）

`parallelize` 对**任何**非 `none` 的 `sp_backend` 都装同一样式（本仓只有一个 SP 实现），
所以 `ulysses` 静默等于 `megatron`。这一条此前已被记录（§5.2 的 MINOR 项），但**它的实际
后果比记录的重**：

**验证矩阵在说谎。** `scripts/parallel_matrix.py` 的注释把两个条目分开描述——

```
fsdp_sp     → "exercises Megatron-style sequence sharding"
fsdp_sp_tp  → "exercises the Ulysses/TP path"
```

而两者的 spec 只在 `sp_backend` 上不同（`megatron` vs `ulysses`），其余参数完全相同。
**实测 loss 逐位相同**（都是 `1.0994951725006104`）——即那个被命名为「Ulysses/TP path」的
条目，从来没有跑过 Ulysses，也没有贡献任何覆盖率。本仓此前报的「矩阵 5/5 通过」里，
这一条是重复计数。

**处置**（用户二选一确认后）：保留枚举取值、在 `ParallelConfig.validate` 里**拒绝**它，
错误信息给出原因与出路；删掉冗余矩阵条目。

拒绝而不是删除取值：留下取值才能让拒绝**自我解释**（删掉的话用户只看到
「unknown value 'ulysses'」，不知道改用 `megatron` 还是该去调 `UlyssesAttention`）。
与 `compile.enabled`、`fp8`、`offload_nvme` 同一个模式。

**为什么不真的把它接上**：头交换需要 `all_to_all`，Gloo 没有实现，所以 world>1 的 Ulysses
路径是 CUDA/NCCL 专属——本机无法验证任何多卡行为。按本仓既有立场（Stage D 的 PP 迁移同理），
不盲写无法验证的代码。Ulysses 的**工具 API**（`UlyssesAttention` / `distributed_attention` /
`all_to_all_layout`）不受影响，world=1 有数值等价测试、`examples/tp_sp_train` 在用。

**连带修正**：
- `capability.py` 的 `sp` 描述原为 "synchronous Ulysses and sequence-parallel paths"，
  改为说明 config 只能选 `megatron`、Ulysses 是需要 NCCL 的独立显式 API
- `scripts/parallel_matrix.py` 删条目、改注释，`fsdp_sp_tp_pp` 的 `sp_backend` 改 `megatron`
- 测试里 3 处 `ulysses` 改 `megatron`（保持各自原本的测试意图）；
  `test_fsdp_boundary` 的参数化从 3 组降到 2 组——删掉的那组与另一组逐字相同
- 新增两条测试：拒绝本身（含错误信息断言，`from_dict` 与 `validate` 两条入口都测到），
  以及**矩阵内不允许两个条目跑同一配置**——后者是让这类冗余无法再溜进来的机制

**反向对照**：去掉拒绝逻辑 → 新测试红；把冗余条目加回去 → 新矩阵测试红，并**精确报出**原因
（`fsdp_sp_tp runs the same configuration as fsdp_sp: (2, 2, 1, 'megatron', 'none', 1)`）。

**顺带修掉 README 的 Parallelism 段**：它把 `ColumnParallelLinear` / `RowParallelLinear` /
`SequenceParallelLayerNorm` 当作现行实现描述，而这三个类**只存在于 docstring 里**（说「曾经是」）。
实际 TP 是 DTensor `parallelize_module`，SP 是 `sequence_parallel_styles` 只包 LayerNorm。
与 `sp_backend` 是同一段文字，一并改了。

**验收**：255 passed；ruff 90（基线未变）；3 契约 kept；4-rank 矩阵 3 条全 `validated`、
`fsdp_run` 参数误差 `0.0`、overlap `passed`；7 示例通过。

---

## 死配置字段的清零，以及一条拦住它的测试（2026-09-16）

`precision.reduce_dtype` 被实测确认**对训练没有任何影响**，四条路径上只改它、结果逐位相同：

| 路径 | float32 vs bfloat16 | 参数类型 |
|---|---|---|
| 单进程 | `0.000e+00` | Tensor |
| FSDP（world=1） | `0.000e+00` | Tensor |
| dp=2 —— FSDP 梯度归约 | `0.000e+00` | DTensor |
| tp=2 —— row 投影 all-reduce | `0.000e+00` | DTensor |

最后一条是它 docstring 明确声称接的地方。**灵敏度对照**：人为把 bf16 接进 FSDP 的
`MixedPrecisionPolicy(reduce_dtype=...)` 后结果改变 `1.69e-02`——所以 `0.000` 是「真的
没接」，不是「测不出来」。四条路径的参数都是 `DTensor`，即分片确实生效。

**它不是从来没接过，而是被迁移掐断的**：`git show 0b28273:src/aitrainer/parallel/tp.py`
里 `RowParallelLinear.__init__(reduce_dtype=...)` → `reduce_from_tp(..., reduce_dtype=self.reduce_dtype)`
是真的接线；迁移提交 `773107d` 把这些行整体删除，换成不接受 dtype 的 `RowwiseParallel`。
**接线走了，字段和关于它的一切都留下了**——字段、`validate()` 的校验项、两个设置它的
preset、以及那句 docstring。校验尤其毒：设一个非法值会**报错**，于是它看起来比真字段还
真。（§5.2 那条「已修」记录的正是迁移前的那次修复，已更新。）

它的净效果是：用 `ConfigPreset.performance()` 或 `memory_saving()` 的人以为开了 bf16
归约，在所有输出里也看到 `'reduce_dtype': 'bfloat16'`，而计算全程 fp32。

**处置**：删除字段、那句 docstring、`validate()` 的校验项、两个 preset 里的设置，以及
`examples/config.json` 里的一行。

### 顺带删掉同类的最后一个

用 AST 精确扫描「配置字段在 `schema.py` 之外有没有读取点」（属性访问 `.field` 或
`getattr(obj, "field")`；**字段自己的类型校验不算**——那恰恰是死字段唯一剩下的痕迹），
删掉 `reduce_dtype` 后 39 个叶子字段里只剩一个：`overlap.enable_parameter_prefetch`。

它比 `reduce_dtype` 更彻底地死：其余五个高级 overlap 开关至少还进 `capability.py` 的拒绝
守卫（`架构.md` §4 说它们「仅拒绝用」），而这一个**连拒绝作用都没有**，唯一读它的是它自己
的类型检查。对照组 `enable_transfer_overlap` 真的被 `offload/manager.py:29` 读——即那个位置
本该有读取点。一并删除。

### 拦住的测试

`test_every_config_field_is_read_somewhere`：枚举 `FrameworkConfig` 的全部叶子字段，AST 扫描
`src/`，任何一个在 `schema.py` 之外没有读取点就失败并点名。

这条测试值当，因为这个缺陷类型在本仓已出现**至少七次**：`param_dtype`、`reduce_dtype`、
`FSDPConfig.sharding` / `limit_all_gathers` / `forward_prefetch` / `execution_trace_complete`、
`CompileConfig.mode` / `fullgraph` / `dynamic`、`enable_parameter_prefetch`。而它有一个
**没有任何既有测试能看见**的性质：**字段停止被读取时不会失败**。`test_precision` 全绿，
数字全对，只是那个旋钮什么也不做了。

**三条对照**：把 `reduce_dtype` 加回去 → 红；加一个全新的死字段 → 红；加一个**真被读**的
字段（`log_interval`）→ 绿，即不误报。

**验收**：254 passed（+1）；ruff 90（基线未变）；3 契约 kept。

---

## 分片 checkpoint 补齐 scheduler / scaler / sampler（2026-09-16）

上一轮修了分片路径的 RNG，但**同一处缺口还有三个字段**：`save_sharded` 只写
model/optimizer/bookkeeping，而非分片的 `save_checkpoint` 另外存 scheduler、scaler 和
sampler 位置。这一轮补齐，两个格式现在携带同样的内容。

**scheduler 缺口的实测**（`CosineAnnealingLR(T_max=10)`，跑 4 步后各续训 2 步）：

| 来源 | `last_epoch` | 续训 2 步后的 lr |
|---|---|---|
| 参考 run | 4 | 应为 0.034549 |
| per-rank 格式 | 6 | **0.034549** ✓ |
| 分片格式 | 2 | **0.059201** ✗ |

**为什么此前没被发现**：`lr` 随 optimizer 的 `param_groups` 回来了，而 `StepLR` 是在
**当前 lr** 上乘系数、不是从 `last_epoch` 算——所以 StepLR 的用户**碰巧**完全正确。
第一版探针我用 StepLR 测，两种格式给出同样的结果，差点得出「没问题」的结论；换成按
`last_epoch` 算绝对值的调度器才暴露。**测一个性质时要选能让它失败的对象。**

**sampler 只能暂存**：provider 是 `fit` 的 `data` 参数，续训的写法是先 `load_sharded()` 再
`fit(data)`，所以加载时没有 provider 可写。其状态以 **bytes**（非张量）存进 DCP——实测
DCP 对非张量值是**整体替换**（列表长度、甚至类型不一致都不校验，占位写什么都行），张量
才必须与存档形状完全一致。`load_sharded` 把它挂在 trainer 上，由**恰好一次** `fit` 取用。

### 顺带加的守卫，以及第一版写错了

DCP 按 recipe 逐键填充：checkpoint 里有、recipe 里没有的条目不报错、只是不读。所以
「存了、没认领」会丢掉一个命名空间而看起来一切正常。加载前比对键集即可。

**第一版守卫按「键」比较，被实测否掉**：分片 checkpoint 在各 rank 上是**分区**的，而
`read_metadata()` 报**全局**键集——流水线 stage 持有 `model.unit0.*` 而元数据里还有 stage 1
的 `model.unit2.*`，RNG 按坐标命名空间。于是每个正常 checkpoint 都被拒：实测 `dp2-pp2`
下 48 条误报、`dp2-tp2-pp2` 同样红，7 条既有测试变红。改成按**命名空间**比较（`model.`
`rng.` `scheduler` …）后恰好抓住真正发生过的那类失败。**误报和漏报一样是缺陷，只是更早暴露。**

`_flatten_keys` 照 `torch/.../_traverse.py` 重写（不 import 私有模块），
`test_flatten_keys_matches_torch` 用真实存档把两者钉住。规则里唯一不显然的是**列表何时算
一个值**：DCP 只在列表装着可下钻内容时才下钻，`base_lrs=[0.1, 0.1]` 是**一个**键。

### 顺带修掉的文档漂移

同一节里 `架构.md` 第 194 行仍把 `checkpointing._stage_prefix` 当成 PP 的现行机制，而
第 275 行自己写着「已删除」——同文自相矛盾。已改为描述真正的原因（`PipelineSequence`
保留原名，各 stage 的 key 天然不撞）。

**反向对照（4 条都做过）**：保存侧去掉 scheduler → 单卡与多卡两条都红；去掉 sampler →
红；去掉守卫 → 红；把 `_flatten_keys` 的列表规则改错 → 钉住它的那条红。

**验收**：253 passed（+4）；ruff 90（基线未变）；3 契约 kept；7 示例通过；
随机状态审计 `failures: 0`。

---

## 随机状态审计，以及它查出的两个静默缺陷（2026-09-16）

用户提出的问题：多机多卡、尤其组合轴下，随机状态最容易静默出错——FSDP full-shard 时各机
初始化的权重可能不一致，相同 sp_rank 的随机流可能不同步，于是数据加载不一致、随机算子
不一致，训练不收敛而**没有任何报错**。要求分析各分片策略的潜在问题，并给出**可运行的验证脚本**。

交付 `scripts/verify_random_state.py`：显式 rendezvous 起真进程，逐项检查并打印**每个 rank**
的一行结论（失败时点名轴与 rank，而不是「训练发散了」）。2 rank 实测：

| 检查 | 结果 |
|---|---|
| `init` —— 各 rank 建出的模型 | ✓ `seed=1234`，逐位相同 |
| `rng-sync` —— forward 前后各 rank 的 RNG | ✓ 同步 |
| `sp-dropout` —— SP 区域 vs 单进程 | ✓ `5.96e-08` |
| `data-shard` —— dp 各 rank 能否分片 | ✗ → 已修 |
| `checkpoint-rng` —— 续训后的随机流 | 非分片 ✓ / 分片 ✗ → 已修 |

审计本身查出的**两个静默缺陷**（详述见 `架构.md` §6）：

1. **数据没有按 dp rank 分片。** `loop.py` / `evaluation.py` 恒传 `dp_group=None`，provider
   拿不到 rank，只能每个 rank 遍历整个数据集——数据并行退化成 N 份相同梯度的平均，花 N 倍
   算力换一个 rank 的批次。修法：`Trainer.dp_process_group()` 复用 `parallelize` 已建好的
   mesh（**不新建**——`new_group` 是集合操作，重复一份轻则浪费、重则顺序不同而挂死）。
2. **分片 checkpoint 不保存 RNG。** 两个格式现在都存 `torch`+`python` 两个流并都恢复。

两个具体做法不是显然的，记下来：

- **RNG 状态按坐标命名空间化**（key = `rng.{pp}.{tp}.{dp}`）：随机状态是 per-rank 的，各 rank
  本就不同，共用 key 会让 DCP 把某一个 rank 的值当成**唯一**的值发给所有人。
- **Python 的 RNG 状态不能靠 pickle。** DCP 用交给它的 state dict 的**形状**决定读多少字节，
  所以占位与保存的形状必须一致，而 `random.getstate()` 的 pickle **长度会变**（缓存 gauss 从
  `None` 变成 float）——实测保存 3752 字节、加载 3829 字节，DCP 报 `Size mismatch between saved
  torch.Size([3752]) and current torch.Size([3829])`。改为拆开存 version + 625 个 word + gauss
  （`None` 记 NaN），形状全固定。

**两条负面教训，都已写进测试注释与 `架构.md`：**

- **检查 helper 而漏掉调用点。** 只断言 `dp_process_group()` 非 None，即使 `fit` 仍传 `None`
  也全绿。现在多卡用例用一个**记录型 provider** 报告 `fit`/`evaluate` 实际传下去的是什么，
  并断言各 rank 的 `dist.get_rank(group)` 互不相同（provider 就是靠这个分片的）。
- **测试自己写错了。** `test_sharded_checkpoint_restores_the_random_stream` 第一版在**同一次
  run** 里先抽 `expected` 再保存——保存下来的流已经越过那些抽取，于是与完全正确的实现比较
  也不等，把好实现报成了坏的。正确形状是两次相同 run：第一次当参考，第二次 fit 到同一点、
  保存、把流挪走、再加载。

**反向对照（每条都做过）**：`save_sharded` 去掉 RNG 键 → 红（DCP 显式报缺 key）；两侧都去掉
→ 断言红，不是静默；`fit` 改回 `dp_group=None` → 红。`scripts/verify_random_state.py` 现已同时
测量 torch 与 python 两个流。

**验收**：249 passed（+2）；ruff 90（基线未变）；3 契约 kept；审计 `failures: 0`。

---

## 修复状态（2026-09-15 更新）

Stage 1–4 共 **36 项已修复**，其后在真实多进程作业下验证时又发现并修复 **2 项**（见「本轮新发现」）。验收对比：

| 指标 | 修复前 | 修复后 |
|---|---|---|
| `pytest -q` | **10 failed**, 71 passed | **0 failed**, **183 passed** |
| 7 个示例 | 3 个崩溃（tp/pp/offload） | **7 个全部通过**（`fsdp_train` 见下） |
| TP/SP 六条路径 | 全部 `TypeError` | 全部通过 |
| `ruff check src tests examples` | 152 个错误 | **143 个**（净减 9，未新增） |
| 文档化的 CLI 流程 | `validate --config config.json` 无配置可指 | `examples/config.json` 随仓库提供且校验通过 |
| 真实多进程作业 | **从未执行过一次** | `tests/unit/test_multirank_gloo.py` 11 项，每项起真进程 |

### 已修复并实测验证（单进程可验证的全部条目）

§2.1 collectives 参数数（3 处）、§2.1b `reduce_dtype` 作用域、§2.2 元组 batch 契约（含被其掩盖的 Mapping 分支缺 reshape）、§2.3 overlap 基线记账、§3.1 PP 累积步数除法、§3.2 参数 offload KeyError 与同形参数塌陷、§3.3 `apply_candidate` 字段丢失、§3.4 `suggest_plan` 括号与 world_size、§3.5 梯度桶每窗口归约、§3.6 静态 P2P 对端、§3.7 `evaluate()` 跨 rank 归约与 NaN、§3.8 种子时序、§3.9 `dry_run` world_size、§4.1 `no_sync` 接线与守卫加固、§4.2 TP-only 可训练、§4.3 `_enforce_budget`、§4.4 `mesh.py` 吞异常、§4.5 flush 裁剪与残留梯度、§4.6 GradScaler 跳过、§4.7 checkpoint 四处、§4.8 `MixedPrecisionConfig.dtype` 校验、§4.9 `from_dict` 嵌套校验、§4.10 h/i/k/l/m/n/o。

### 已用**进程内多 rank 模拟器**验证（`tests/unit/test_pp_schedule_protocol.py`，17 项）

`P2PCommunicator` 是可替换的，所以用消息队列伪造一个通信器就能让**真实的** stage 代码在所有 rank 上跑起来，并由伪造器执行与真实 header **相同**的元数据校验（stage + microbatch 必须与接收方期望一致）。这覆盖了原先判为"只能盲修"的整类协议缺陷：

- **§4.10 e**（GPipe 反向顺序）—— 断言每个发送方的 microbatch **降序**。修复前末级在 forward 循环内发送（升序），而所有接收方按 `reversed(fifo)` 排空 → M≥2 必错
- **§4.10 f**（1F1B 稳态）—— 直接移植 `PipelineStage._pipeline_step_1f1b` 这一经审计验证自洽的实现。**`M > pp_size` 这一官方推荐区间原先死锁**，现于 pp=2/3/4、M=2/3/4 全部通过，且每 rank 恰好 M 次前向、M 次反向
- **§4.10 g**（stage 元数据）—— 断言每个发送方的 microbatch **升序/降序**与接收端一致，且接收端改按**本级** stage id 校验
- **负向对照** `test_the_harness_detects_a_mislabelled_gradient`：证明这套校验**是活的** —— 修复前的期望值会被拒绝，修复后的会通过。没有这条，绿色测试什么也证明不了
- **顺带发现的真实缺陷**：`PipelineSchedule.__init__` 的 `stage_id` 默认 `0` 且与 `communicator.pp_rank` 无关，任何漏传该参数的 stage 都会把自己的前向标记成 "stage 0"（pp=2 时恰好是邻居而不报错）。已改为默认取 `communicator.pp_rank`，并对显式传入的不一致值报错

**模拟器须忠实**：第一版伪造器直接把发送方的张量传给接收方，导致两个 stage 落在**同一张** autograd 图上、下游 backward 释放了上游的图 —— 那是伪造器的假象，不是调度缺陷。真实 P2P 接收会分配新缓冲并标记为叶子（`requires_grad_(True)`），伪造器现已照此实现。

### 真实多进程验证（新增，`tests/unit/test_multirank_gloo.py`，11 项）

原先记为"只能盲修"的多卡项已**在真实多进程 Gloo 作业下逐条验证**。基础设施是 `tests/multirank/`：每个 rank 起一个真 OS 进程（显式 `MASTER_ADDR`/`RANK`/`WORLD_SIZE`），生产代码在活进程组上运行，每个 rank 回传 `RESULT <json>` 供断言。

**为什么值得建**：`tests/distributed/` 下 6 个文件**全部是单进程**——它们只做 `importorskip("torch")` 然后断言 `tp_size == 1` 的形状。**仓库历史上从未创建过第二个进程**，所以"多卡未验证"不是环境所限，而是从来没有测试去验证。

| 条目 | 验证结果 |
|---|---|
| **§4.10 a** `new_group` 顺序 | 4 rank（pp=2/dp=2、pp=2/tp=2、dp=2/tp=2 三种 mesh）全部 0.8s 内完成，每组归属与 `group_ranks` 一致。**负向对照**：把修复前的逐 rank 顺序原样跑一遍 → `DistStoreError: wait timeout after 8000ms, keys: /default_pg/0//2//cpu//0/rank_0`，另两个 rank 挂死。修复前顺序**确实死锁**，此前的 A/B 结论被独立复现 |
| **§4.10 b** gather_output+bias | 与 dense `nn.Linear` 对比：forward `7.5e-8`、`skip_bias_add` 融合路径 `7.5e-8`、输入梯度 `1.2e-7`。**反向对照**：把 bias 挪回 gather 之后 → `The size of tensor a (12) must match the size of tensor b (6)`，正是 tp_size 倍宽度差 |
| **§4.10 c** SP LayerNorm 梯度归约 | forward 误差 **0.0**、weight 梯度 `2.4e-7`、bias 梯度 **0.0**（对 dense 参考）。**反向对照**：去掉归约钩子 → 两 rank 梯度差 `1.74 vs -1.39`，证明归约确实是必需的 |
| **§4.10 d** 变长 Ulysses | world=2 下两个 rank 都抛 `SPConfigurationError`，消息含 `seq_lens=None` |
| **§4.1** `no_sync` 累积 | 真正的 `Trainer.fit` + 真 FSDP + 真 DP 组：7 个 microbatch / accumulation=3 → `applied == [F,F,T,F,F,T,F]`、2 次优化器步、尾部残窗被丢弃。world=1 参考与 world=2 两个 rank **逐位相同**（`max|dW| = 0.000e+00`）。**反向对照**：把 `fit()` 末尾的 flush 加回去 → world=2 直接崩 |

**这是本仓库第一次真正跑起来的多卡训练。** 顺带修正一条此前写错的方法学结论：`torchrun` 确实不可用（macOS 主机名 mDNS `gai error: 8`），但**多卡本身完全可跑**——把 rendezvous 变量显式写进环境即可。二者不可混为一谈：前者是 `torchrun` 的限制，后者根本不存在。

### 本轮新发现并修复的缺陷（都是真实多进程下才暴露的）

- **`wrap_fsdp` 只在设备以 `"cuda"` 开头时才转发 `device_id`**（`parallel/fsdp.py:136`）——CPU 上 `device_id=None` 让 FSDP 自行推断设备，在 macOS 上解析到自定义 `mps` 后端并抛 `AttributeError: Custom backend 'mps' not implement 'torch.mps.current_device'`。后果：**被标为 `STABLE` 的 `fsdp_full_shard` 及其全部组合在任何纯 CPU 机器上根本无法初始化**，而报错信息既没提 FSDP 也没提缺的是哪个参数。已改为转发 runtime 实际解析出的设备（`Runtime.resolve_device` 只会返回 `"cpu"`/`"cuda:N"`，不存在需要过滤的 `"auto"`）。修后 `examples/fsdp_train/train.py` **在 2 rank 下跑通**（`completed 4 optimizer steps`，两 rank 均 rc=0）——此前它被记为"需要 torchrun"，实际是根本没跑起来过。
- **`FSDPWrapper.full_state_dict_context()` 在 CPU 参数下段错误**（`parallel/fsdp.py:101`）——它硬编码 `FullStateDictConfig(offload_to_cpu=True, rank0_only=False)`。实测 world=2 Gloo：`offload_to_cpu=True`（两种 `rank0_only`）**直接 SIGSEGV，无 traceback**；`offload_to_cpu=False` 正常返回完整张量。已改为按参数实际设备决定 `offload_to_cpu`。该方法在 `src/` 内**没有任何调用方**（"导出但无消费者"），故此前无人踩到——但它是公开类的公开方法，任何调用者都会拿到一个没有 traceback 的硬崩溃。
- **Ulysses 在 Gloo 上完全不可用**：`all_to_all` 没有 Gloo 实现，world≥2 一律抛 `RuntimeError: Backend gloo does not support alltoall`。**所以整条 Ulysses 路径实际是 CUDA/NCCL-only，仓库没有任何地方写明这一点**；`distributed_attention` 的 dense 等价性检查因此无法在纯 CPU 机器上运行（已写成显式测试 `test_ulysses_head_exchange_requires_nccl_not_gloo`，等 Gloo 支持 all-to-all 时它会失败并提示改跑等价性检查）。

### 仍未验证的部分

- **§4.10 o**（`rms_norm` bf16 精度）与 `k/l/m/n`、`h/i` 已在单进程下验证，无需多卡
- **CUDA/NCCL 专属路径**：真实 mixed-precision 缩放、`Ulysses` 数值等价性、激活 offload 的 `_eligible`（对 CPU 张量返回 False，本机 `offloaded=0`）
- 服务器上仍建议跑一次全矩阵：`PYTHON=/path/to/venv/bin/python bash scripts/run_all_tests.sh --world-size 4 --report-dir artifacts/server_report`

### 修复后的第三轮审查（4 个独立 reviewer，逐条实测复核）

对**修复后的代码**再做一轮审查，结论：**缺陷密度最高的地方是我自己的改动**。三个 HIGH 里有两个是我在第二、三轮引入的回归。

**HIGH · 我引入的回归**
- `no_sync` 接线（Stage 3）导致 `fit()` 收尾的 flush 踩在**未归约**的梯度上：尾部 microbatch 是在 `no_sync()` 下累积的，2 进程 DDP 实测副本永久发散（窗口内 dev 恒 0.0，`fit([])` 之后 dev=5.852e-02）。**flush 已删除**（部分窗口直接丢弃），同时修掉其错缩放（k/G vs k/k）、无梯度时的幽灵 LR 推进，以及异常路径遗留 `.grad` 导致的重试重复累加
- reader 的"无匹配分片就报错"（Stage 3 B7②）对**属于其它 PP stage** 的张量也触发，废掉了同轮加入的 `stage_assignment` 逐 stage 加载。改为按"模型是否拥有该张量"区分：不拥有 → 跳过；拥有却无分片 → 响亮报错
- `_publish` 对 FILE/SYMLINK 目标做 `rmtree`：保存已发布却抛异常并**永久毒化路径**。改为类型感知清理 + 跟随 symlink
- `groups.py` 把 `world_size==1` 早退放在 world-size 校验之前 → 返回 `None` 组，而 `dist` 把 `None` 当成**全体 rank**。已调整顺序

**HIGH · 既有问题（非本轮引入）**
- **`Trainer.__init__` 从不调用 `validate_capabilities`**：FSDP + gradient-bucket 冲突经 `Trainer(...)` **静默通过**，而 `dry_run`/`from_model` 会拒绝。已在 `__init__` 中补上校验，使能力门控与入口无关
- **`distributed_model.py` 的元组 batch 回退用 `mse_loss`**，而 `:123`/`dict` 分支/`_compute_loss`/`PipelineSchedule._loss` 全部用 `cross_entropy` → **开启 PP 会静默把分类目标换成回归目标**。已统一为 cross-entropy

**MEDIUM · 拒绝而非静默错误**
- **Ulysses `seq_lens` 在 world≥2 给出错误注意力**（对 dense SDPA 最大误差 **0.42**，而 `seq_lens=None` 为 2.4e-7）：根因是 `pad_sequence` 把**全局**最大长度套用到**本地**分片。该路径无法在不重新设计的情况下修正，故**改为响亮拒绝**（world≥2 且传 seq_lens 时抛 `SPConfigurationError`），并修正 `pad_sequence` 的单位问题。world=1 行为不变
- `run_last_stage`：空列表 `ZeroDivisionError`、`zip` 长度不匹配静默截断（上游会阻塞在 recv_backward）→ 均改为报错/返回 None

**其他已修**：README 的 overlap 措辞（**我写反了**：这些开关不是"改变记账"而是**完全无效果**，其中两个连 `any((...))` 都没进）、`run_all_tests.sh` 硬编码 `python3`（现支持 `$PYTHON`）、`capability.py` 的死分支、`presets` 设置死字段的误导、`wait_posted` 接受却丢弃 timeout（已移除该参数）、`backward_complete` 对字符串 schedule 的 `getattr` 死分支。

**明确未修**：`MetricStore`/`TPBulkOverlap`/`TransferScheduler` 等**导出但无消费者**的 API（§5 已归类为新功能）、`CheckpointConfig.interval` 等死字段（已在 `config.py` 注明）、`sp_backend` 取值被忽略（2 进程 gloo 实测 `ulysses` 与 `megatron` 得到逐位相同的 loss）。

**第二轮审查追加修复**
- **HIGH · 又一处我的回归**：`ConfigPreset.memory_saving()` 曾在**第一步就崩**（`PrecisionError`）。我在 Stage D 为了区分两个 preset 给它设了 `grad_dtype="bfloat16"`，但 `param_dtype` 是死字段（参数仍是 fp32），而 `cast_gradients` 要求梯度与参数 dtype 一致 → 该组合永不可满足。现改为在**活的** `optimizer_dtype` 上区分（memory_saving 用 bf16 优化器状态，Adam 两个动量减半），`grad_dtype` 保持 fp32
- **`cast_optimizer_state` 会连带转换 `step` 计数器**（审计 §4 第 15 条，此前未修）：Adam 把 `step` 存为 float32 张量，dtype 过滤把它一起降精度，与函数自身"preserving integer counters"的契约矛盾、越过 2^8 后 bias correction 失真、并会让断言 step dtype 的 fused CUDA Adam 报错。现按 key 跳过 `step`
- **bf16 在 CPU 上静默跑 fp32 且无任何提示**：唯一的 bf16 检查是 `capability.py` 里那个死分支（本轮已删）。现在 `Trainer.__init__` 会发出 `RuntimeWarning`
- **文档漂移修正**：`docs/设计文档.md` 引用了**一堆不存在的符号**，其中 `aitrainer.backends` / `aitrainer.experimental` 会直接 `ModuleNotFoundError`，`sp_enabled` 字段名应为 `sp_backend`（照抄会得到 `unknown configuration fields`），`DeviceMeshManager.tp_group` 实为 `tensor_parallel_group`。设计文档自己声明代码片段不可直接运行，但**字段名与模块路径**这类容易被照抄的内容仍应视为待修
- **`GPipeSchedule`/`OneFOneBSchedule` 相对 `Trainer` 是死代码**：`parallelize` 传的是**字符串**，`Trainer` 走 `PipelineStage.pipeline_step`（另一套实现），两个 `_run_distributed` 与 `run_last_stage`/`run_middle_stage` 在 `src/` 内不可达；多卡下唯一被真正执行的调度是 `distributed_model` 那套
- **「可复现」的示例其实不可复现**（本报告自己文档化的陷阱，示例却踩了）：`examples/tiny_transformer/train.py` 在 `Trainer(...)` **之前**构造模型，而种子是在 `Trainer.__init__` 里构造 Runtime 时才生效 —— 实测 5 次运行得到 5 个不同 loss（4.9764/4.9738/5.0157/5.0875/4.9409）。数据生成器是显式播种的，这让示例**看起来**受种子控制。现已在建模型前显式 `seed_everything(config.seed)`，5 次运行得到同一个 loss=5.1747；并加了跑两遍比对 loss 的回归测试
- **我替换 README overlap 段落时又写错了一个量词**：新段落说"启用其中任何一个都会得到与全关基线逐字节相同的结果"，但 7 个开关里有 3 个（`tp_bulk_overlap`/`pp_p2p_overlap`/`microbatch_interleave`）在默认单卡拓扑下是**构造即报错**，而非得到相同基线。已改为按轴限定，并注明哪三个会中止构造
- **runner 文档三处不实**：缺 `torchrun` 时 runner 会**回退**到 `python -m torch.distributed.run` 而**不是**记为 blocked（README 却把 torchrun 列在"reported as blocked/skipped"里）；`report.html` 通过**相对 href** 链接 `logs/` 里的文件，并非"self-contained"；README 只写了 4 个 flag，实际有 15 个左右（含 `--skip-distributed`/`--skip-parallel-matrix`，会跳过整个矩阵验证）
- **`enable_pp_p2p_overlap` 两条 P2P 路径都不受它控制**（其唯一可观察效果是 pp_size≤1 时报错）—— capability 理由已改为如实描述
- **变量长度 SP 的表述**：`seq_lens` 在 world≥2 的**响亮拒绝**是刻意且有据的工程决策（原实现返回错误注意力，误差 0.42），不是缺陷；README 已写明该路径仅 world==1 可用

**方法学更新（重要）**：`torchrun` 在本机不可用（macOS 主机名解析），但**多卡本身是可跑的** —— 用显式 `MASTER_ADDR/RANK/WORLD_SIZE` 手工起进程即可，reviewer 已用 2 进程 gloo 实测 `tp=2`（两 rank loss 一致）。所以"无法验证多卡"这一限制**只适用于 torchrun**，不适用于多卡本身；FSDP 初始化仍因 torch 内部的 mps 设备句柄问题失败（环境因素）。

### §4.10 d/e/f/g 的复现结论（重要）

审计时这四条**未能在本机复现**（d 在 ws=1 下只报合理的语义守卫；e/f/g 需 pp≥2，而这两个 schedule 类无任何 in-tree 多卡调用方）。本次是按分审计的分析**盲修**，请在服务器上优先验证。

### 未修复

- `sp_backend` 取值被忽略（`ulysses` 与 `megatron` 产生相同计算图，HuggingFace 的 `UlyssesAttention` 未接入任何模型）—— 分审计的 MINOR 项
- §5 中不属"缺陷"而属"新功能"的条目（真实数据集路径、`Profiler` 接线、预训练权重加载、推理/导出）

### 审计遗漏的新发现

CI 的 `ruff check src tests examples` 有 **152 个既有错误**（I001/UP035/UP037/RUF100/F401 等，140 个可自动修复）—— CI 不仅在 pytest 上会红，**在 ruff 上也会红**。本次修复**净减少 7 个**（152 → 145），且每加一个都由 `ruff --fix` 清掉，未新增任何错误。

---

## 0. 如何复现本文所有结论

审计环境：Python 3.12.13 + torch 2.9.1 + pytest 9.1.1。

> ⚠️ 系统自带的 `python3`(3.9.6) 与 `python3.12` 均无 torch，而 `pyproject.toml` 要求 `requires-python = ">=3.10"`。本文所有实跑使用一个已装有 torch 的既有虚拟环境（`SyncEdit/.venv`）。**仓库自身从未在此环境下运行过。**

```bash
# 约定：$PY 指向装了 torch>=2.1 与 pytest 的解释器
cd /Users/linzhihui/Documents/codes/aiTrainer
export PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src

# ① 测试套件（红 10 个）
$PY -m pytest -q

# ② TP/SP 在任何 world size 下崩溃（无需多卡）
$PY -c "
import torch
from aitrainer.parallel.tp import RowParallelLinear
RowParallelLinear(4, 4, process_group=None)(torch.randn(2, 4))
"

# ③ 文档示例
for ex in tiny_transformer tp_train pp_train offload_train; do $PY examples/$ex/train.py; done
```

验证标记：
| 标记 | 含义 |
|---|---|
| ✅**实测** | 审计者用真实 torch 跑出来的结果 |
| 📖**代码确证** | 阅读代码即可判定，无歧义 |
| 🔍**分审计** | 由分块审计智能体报告并附自有证据（**未**经审计者独立复跑） |

---

## 1. 最重要的事实：测试套件在装 torch 后红 10 个

```
$ $PY -m pytest -q
10 failed, 71 passed in 0.99s        # ✅实测
```

CI（`.github/workflows/ci.yml`）只跑 `ruff` + `scripts/check_no_reference_imports.py` + `pytest -q`，其中 `pytest` 会安装 `torch`（它是 `pyproject.toml` 的硬依赖 `dependencies = ["torch>=2.1"]`）。**因此 CI 一旦执行就会红。而该仓库不是 git 仓库（无 `.git`），所以这份 CI 从未执行过。**

### 1.1 十个失败测试清单

| # | 测试 | 失败原因（一行） | 性质 |
|---|---|---|---|
| 1 | `tests/distributed/tp/test_tp_boundary.py::test_tp_modules_construct_at_tp1` | `TypeError: _Scatter.apply.<locals>.Function.forward() takes 2 positional arguments but 4 were given` | 框架缺陷 A |
| 2 | `tests/performance/test_forward_backward_correctness.py::test_trainer_step_updates_parameters_and_returns_finite_loss` | `TypeError: Sequential.forward() takes 2 positional arguments but 3 were given` | 框架缺陷 B |
| 3 | `tests/performance/test_forward_backward_correctness.py::test_optimizer_offload_preserves_one_step_result` | 同上 | 框架缺陷 B |
| 4 | `tests/unit/test_overlap_controller.py::test_overlap_controller_records_synchronous_baseline` | `assert 0.0 == 1.0`（`test_overlap_controller.py:8`） | 框架缺陷 C |
| 5 | `tests/unit/test_pp_schedule.py::test_single_stage_schedule_completes_backward[GPipeSchedule]` | `TypeError: Linear.forward() takes 2 positional arguments but 3 were given` | 框架缺陷 B |
| 6 | `tests/unit/test_pp_schedule.py::test_single_stage_schedule_completes_backward[OneFOneBSchedule]` | 同上 | 框架缺陷 B |
| 7 | `tests/unit/test_precision.py::test_precision_dtype_strings_resolve_without_torch` | `AssertionError: assert torch.float32 == 'float32'`（`test_precision.py:8`） | 测试只对无 torch 环境成立 |
| 8 | `tests/unit/test_sp_single.py::test_sequence_parallel_tp1_preserves_shape_and_values` | `TypeError: _Scatter.apply...takes 2 positional arguments but 4 were given` | 框架缺陷 A |
| 9 | `tests/unit/test_tp_single.py::test_tp1_linear_matches_dense_parameters` | `TypeError: _Gather.apply...takes 2 positional arguments but 4 were given` | 框架缺陷 A |
| 10 | `tests/unit/test_trainer_contract.py::test_single_process_trainer_checkpoint_continues_step` | `TypeError: Linear.forward() takes 2 positional arguments but 3 were given` | 框架缺陷 B |

### 1.2 三个根因，全部是框架缺陷

**根因 A（测试 1 / 8 / 9）** —— `torch.autograd.Function.apply` 参数数不匹配，详见 §2.1。

**根因 B（测试 2 / 3 / 5 / 6 / 10）** —— `Trainer.train_step` 把标签一并解包进了 `forward`，详见 §2.2。**这是最关键的一条**：它同时导致 `examples/pp_train`、`examples/offload_train` 崩溃。

**根因 C（测试 4）** —— `OverlapController.submit()` 在 disabled 时不做任何记账，详见 §2.3。

**测试 7** 不是框架 bug：该测试断言 `resolve_dtype("float32")` 返回**字符串** `'float32'`（无 torch 环境下的行为），有 torch 时返回 `torch.float32`。它**只在无 torch 的机器上通过** —— 反向印证了整个测试套件只在无 torch 环境下跑过。

---

## 2. P0 缺陷：装上 torch 就崩

### 2.1 TP / SP 全体在第一行代码就崩（即使 `world_size=1`）

**证据链**（`src/aitrainer/parallel/collectives.py`）：

| 位置 | 代码 | 问题 |
|---|---|---|
| `:130` | `def apply(value, dim, group)` | 声明 3 个参数 |
| `:134` | `def forward(ctx, x)` | `forward` 只接受 **1 个**张量 |
| `:151` | `return Function.apply(value, dim, group)` | 向 `apply` 传 **3 个**参数 |

`torch.autograd.Function.apply` 会把它收到的**全部**位置参数转发给 `forward`，于是实际调用 `forward(ctx, value, dim, group)` → `TypeError`。

同一文件的三处：

| 类 | `apply` 定义 | `forward` 定义 | `Function.apply(...)` 调用 |
|---|---|---|---|
| `_ReduceFromTP` | `:103` | `:107` `forward(ctx, x)` | `:125` `Function.apply(value, reduce_dtype)` ❌ |
| `_Scatter` | `:130` | `:134` `forward(ctx, x)` | `:151` `Function.apply(value, dim, group)` ❌ |
| `_Gather` | `:156` | `:160` `forward(ctx, x)` | `:167` `Function.apply(value, dim, group)` ❌ |
| `_CopyToTP` | `:82` | — | `:98` `Function.apply(value)` ✅ 正确 |

**复现步骤**（✅实测，`world_size=1`，无需分布式）：

```bash
$PY -c "
import torch
from aitrainer.parallel.collectives import scatter_to_tp, gather_from_tp, reduce_from_tp
from aitrainer.parallel.tp import ColumnParallelLinear, RowParallelLinear
from aitrainer.parallel.sp_sequence import SequenceParallelLayerNorm
x = torch.arange(16, dtype=torch.float32).reshape(4, 4)
for label, fn in [
  ('scatter_to_tp',       lambda: scatter_to_tp(x, None, dim=-1)),
  ('gather_from_tp',      lambda: gather_from_tp(x, None, dim=-1)),
  ('reduce_from_tp',      lambda: reduce_from_tp(x, None)),
  ('ColumnParallelLinear(..., gather_output=True)', lambda: ColumnParallelLinear(4,4,gather_output=True, process_group=None)(x)),
  ('RowParallelLinear',   lambda: RowParallelLinear(4, 4, process_group=None)(x)),
  ('SequenceParallelLayerNorm', lambda: SequenceParallelLayerNorm([4], process_group=None, input_is_parallel=False, gather_output=True)(x)),
]:
    try: fn(); print('[OK]   ', label)
    except Exception as e: print(f'[{type(e).__name__}] {label}: {e}')
"
```

实测输出（全部 6 项失败）：

```
[TypeError] scatter_to_tp: _Scatter.apply.<locals>.Function.forward() takes 2 positional arguments but 4 were given
[TypeError] gather_from_tp: _Gather.apply.<locals>.Function.forward() takes 2 positional arguments but 4 were given
[TypeError] reduce_from_tp: _ReduceFromTP.apply.<locals>.Function.forward() takes 2 positional arguments but 3 were given
[TypeError] ColumnParallelLinear(...): _Gather.apply...takes 2 positional arguments but 4 were given
[TypeError] RowParallelLinear: _Scatter.apply...takes 2 positional arguments but 4 were given
[TypeError] SequenceParallelLayerNorm: _Scatter.apply...takes 2 positional arguments but 4 were given
```

**影响**：`capability_matrix()` 把 `tp` 与 `sp` 标为 `STABLE`，README Batch 2/3 称其已交付。**实际状态为零可用。** 修法：把 `forward` 声明为 `(ctx, x, dim, group)` / `(ctx, x, reduce_dtype)`，或只传 `value`（`dim`/`group`/`reduce_dtype` 已是闭包变量）。

### 2.2 元组 batch 的标签被解包进 `forward`（根因 B）

**证据链**：框架自己的测试定义了意图，而实现与之矛盾。

`tests/performance/test_forward_backward_correctness.py:44-48` 定义契约：

```python
trainer = Trainer(model, optimizer, config=..., 
                  loss_fn=lambda output, batch: F.cross_entropy(output, batch[1]))   # 标签由 loss_fn 从 batch[1] 取
inputs, labels = torch.randn(16, 12), torch.randint(0, 5, (16,))
result = trainer.train_step((inputs, labels))    # 模型是 nn.Sequential，forward 只接受一个输入
```

`loss_fn` 自己从 `batch[1]` 取标签 —— 说明**意图是模型只接收 `inputs`**。但 `src/aitrainer/trainer.py:212-215`：

```python
elif isinstance(batch, (tuple, list)):
    output = self.model(*batch)      # ← 把 labels 也当位置参数传进 forward
```

于是 `nn.Sequential(inputs, labels)` → `TypeError: Sequential.forward() takes 2 positional arguments but 3 were given`。

同一模式出现在 `tests/unit/test_pp_schedule.py:19-20`（`nn.Linear` + `(x, y)`）与 `tests/unit/test_trainer_contract.py:16-18`。

**复现步骤**（✅实测）：

```bash
$PY -c "
import torch
from aitrainer import FrameworkConfig, Trainer
model = torch.nn.Sequential(torch.nn.Linear(12,24), torch.nn.Tanh(), torch.nn.Linear(24,5))
trainer = Trainer(model, torch.optim.SGD(model.parameters(), lr=0.05), config=FrameworkConfig(device='cpu'),
                  loss_fn=lambda output, batch: torch.nn.functional.cross_entropy(output, batch[1]))
trainer.train_step((torch.randn(16,12), torch.randint(0,5,(16,))))
"
# TypeError: Sequential.forward() takes 2 positional arguments but 3 were given
```

**影响**：`train_step((x, y))` 是**文档主推的用法**，也是 `examples/offload_train`（`trainer.train_step((x, y))`）与 `examples/pp_train` 的写法 —— **两个示例因此崩溃**（✅实测）。全仓库只有 `TinyTransformer`（其 `forward(tokens, labels)` 恰好接受 2 个参数）能走通，这解释了为什么 7 个示例里只有 `tiny_transformer` 真正跑成功。

### 2.3 `OverlapController.submit()` 在 disabled 时不记账（根因 C）

`src/aitrainer/overlap/controller.py:38-42`：

```python
def submit(self, name, operation, *, context=None):
    del context
    if not self.enabled: return operation()          # ← 提前返回
    started = time.perf_counter(); result = operation(); ended = time.perf_counter()
    self.records.append(OverlapRecord(name, started, ended, 0.0))    # ← 只有 enabled 才记录
    return result
```

默认构造 `OverlapController()` 的 `enabled=False`，因此 `summary()["operations"]`（`:53` 由 `len(self.records)` 求得）恒为 `0.0`。

`tests/unit/test_overlap_controller.py:8` 断言 `summary["operations"] == 1.0` → ✅实测 `assert 0.0 == 1.0`。

**影响**：`capability_matrix()` 把 `overlap_metrics` 标为 `STABLE`，理由为 *"explicit synchronous baseline and wait/overlap accounting"* —— 但连 baseline 记账都是空的。

### 2.4 多卡（≥3 rank）作业在组创建阶段死锁

**证据链**（`src/aitrainer/parallel/groups.py:42-45`）：

```python
for axis in ("pp", "dp", "tp"):
    axis_ranks = mapping.group_ranks(axis, coord)   # coord 是「本 rank」的坐标
    groups[axis] = dist.new_group(list(axis_ranks))
```

每个 rank **只创建自己的组**，因此在第 k 次调用时，不同 rank 传入的是**不同的 rank 列表**。

**机制（已从 torch 源码确认）**：默认 `use_local_synchronization=False` 时，`torch/distributed/distributed_c10d.py` 的 `_process_group_name`（`:5015-5024`）返回 `pg_name = str(_world.group_count)` —— 一个**每进程调用计数器**。于是每个 rank 的第 k 次 `new_group` 都落在 store 命名空间 `k` 中，**与它传入哪个 rank 列表无关**。而 `_store_based_barrier`（`:928-993`）为每个 `group_name` 只用一个计数键，每个参与者 `store.add(store_key, 1)`，只有观察到 `worker_count == rendezvous_count`（该组的**大小**）的 rank 才会设置 `last_worker_key`。

→ 当同一索引处的不同 rank 集合**大小不同**时（world=4、pp=1/dp=2/tp=2：计数器 0 → 四个 1-rank 组；1 和 2 → 两个 2-rank 组），共享计数器会**跳过**期望值 → 没有任何 rank 观察到 `worker_count == rendezvous_count` → **没有任何 rank 设置 `last_worker_key`** → 每个参与者阻塞到 gloo 默认超时（1800000 ms = **30 分钟**）后报错。同一索引处多个进程的读回存在竞态，故该问题**间歇性**。

`world_size <= 2` 在实践中安全（首个读回者总能恰好观察到期望计数）；问题从 **3-4 个 rank** 开始。

**复现证据（干净 A/B，同一环境，✅审计者已核验产物）**：

| 分支 | 做法 | 结果 |
|---|---|---|
| 规范顺序 | 所有 rank 以固定顺序创建全部 8 个组 | `canon4.txt` → **4/4** rank 打印 `ALL DONE` |
| 按 rank 顺序 | 完全复刻 `groups.py:42-45`（每 rank 只建自己的 pp/dp/tp 组） | `perrank4.txt` → **0/4** 到达 `ALL DONE`；日志在建组中途停住，2/2 次运行停滞 |

**真实代码路径**（world=4，pp=1/dp=2/tp=2，走 `DeviceMeshManager` → `ProcessGroups.create`）：

```
R4 rank 0: tp_pg [0,1] ok | dp_pg [0,2] ok | pp_pg [0] ok
R4 rank 1: tp_pg [0,1] ok | dp_pg [1,3] ok | pp_pg [1] ok
R4 rank 2: (未打印 — 仍在 new_group 内)
R4 rank 3: (未打印 — 仍在 new_group 内)
... dist.barrier() -> RuntimeError: Timed out waiting 1800000ms for send operation to complete
       (gloo/transport/uv/unbound_buffer.cc:101)
```

该超时错误在产物 `out5.txt` 中可直接读到。

**影响**：≥3 rank 的作业在组创建阶段**死锁**，每次尝试浪费 30 分钟；而 rank 0/1 创建的成员关系**本身是正确的**，极具误导性。这与项目自己的第 3 条设计原则（"拒绝，而不是静默降级"）相悖。

**修复**（均可在源码验证）：传 `use_local_synchronization=True`（此时用 `_hash_ranks_to_str(ranks)` 按 rank 列表命名，`:5018-5019`，冲突消失），或按规范顺序创建全部组（已验证 4/4 通过）。

> **附注（置信度降级）**：曾有假设认为在 NCCL 上会表现为"配对错误的通信器"而非卡住 —— 该变体**未经验证**，应记为 `SUSPECT`。同一 store 竞争的另一后果是某 rank 可能被**另一个**组的签到提前解除阻塞、在其自身组员全部加入前就从 `new_group` 返回，从而得到成员不完整的组；但实测观察到的是**卡住**，不是这个。

---

## 3. P0 缺陷：静默错误（比崩溃更危险）

### 3.1 PP 路径从不除以 `grad_accumulation_steps` → 等效学习率 ×N

**证据链**：两条路径不对称。

| 路径 | 位置 | 是否除以累积步数 |
|---|---|---|
| 非 PP | `trainer.py:220` `scaled_loss = loss / self.config.grad_accumulation_steps` | ✅ |
| PP | `trainer.py:158-198` `_pipeline_train_step` | ❌ 整条路径无等价除法 |

而 `trainer.py:171` 照样按累积边界决定 step：

```python
should_step = self.global_step % self.config.grad_accumulation_steps == 0
```

`docs/设计文档.md:268` 明确写着契约是 `loss = output.loss / grad_accumulation_steps` —— **实现违背自身文档**。

**影响**：`pp_size>1` + `grad_accumulation_steps=N>1` 时，N 个 step 的梯度被**求和**后更新，每次参数更新恰好比 `pp_size=1` 的同配置大 **N 倍**。默认 `grad_accumulation_steps=1` 掩盖了它。此结论由审计者 📖**代码确证**，并由 PP 分审计从独立的进程内模拟路径复现。

### 3.2 参数 offload 首次 `train_step` 即 `KeyError`，功能 100% 不可用

**证据链**（`src/aitrainer/offload/parameter.py`）：

| 位置 | 代码 | 问题 |
|---|---|---|
| `:47` | `key = id(parameter)` | 按 `id` 起键 |
| `:48` | `if key in self._records: return` | 按 `id` 查重 |
| `:53-55` | `if self.pool is not None: key = BufferKey(...)` | **`key` 被重绑** |
| `:59` | `self._records[key] = _ParameterRecord(...)` | **按 `BufferKey` 存储** |
| `:72` | `record = self._records.get(id(parameter))` | 却仍按 `id` 查 → 恒为 `None` |
| `:75` | `record = self._records[id(parameter)]` | → **`KeyError`** |

`OffloadManager` **无条件**传 pool（`offload/manager.py:42-43` `pool=self.pool`），所以 `self.pool is not None` 恒成立；`fetch()`（`:85`）调 `_record()`；`OffloadManager.fetch`（`:74`）转发给它；而 `trainer.py:206` 在**每个 `train_step` 开头**都调用 `self.offload.fetch(self.model.module, ...)`。

**复现**：`OffloadConfig(enabled=True, parameter=True)` 的第一次 `train_step`，在任何前向计算之前抛 `KeyError`。

**旁证**：`_records` 的类型标注仍是 `dict[int, _ParameterRecord]`（`:40`）—— 这是重构漏改的一半。

**同一段代码的第二个缺陷**：两个 shape+dtype 相同的参数（q/k/v 投影这类）算出**同一个 `BufferKey`**，`:59` 会静默覆盖前一条记录，而其池缓冲仍处于租用状态 → N 个同形参数只留 `distinct_shapes` 条记录、却租了 N 个缓冲 → `close()` 时 `pool.clear()` 抛 *"cannot clear a pool with buffers still in use"*。

**备注**：参数/激活 offload 在 `capability.py:56-59` 标为 `EXPERIMENTAL`（仅笼统的 `offload` 为 STABLE，指的其实只是 optimizer offload），标签本身未过度承诺 —— 但 `EXPERIMENTAL` 在此处的含义是"从未被执行过"。`examples/offload_train` 用 `optimizer=True`，恰好绕开了这条坏路径。

### 3.3 `apply_candidate` 静默丢弃序列并行与混合精度

**证据链**（`src/aitrainer/planner.py:139-144`）：用 5 个字段重建 `ParallelConfig`（丢掉 `sp_backend`、`pp_schedule`），并直接 `FSDPConfig(enabled=True)` 全新构造（丢掉 `forward_prefetch`、`execution_trace_complete`、整个 `mixed_precision`）。

**复现步骤**（✅实测）：

```bash
$PY -c "
from aitrainer.config import *
from aitrainer.planner import apply_candidate, PlanCandidate
cfg = FrameworkConfig(
    planning=PlanningConfig(enabled=True, allow_rewrite=True),
    parallel=ParallelConfig(dp_size=2, tp_size=2, sp_backend='ulysses', num_microbatches=4),
    fsdp=FSDPConfig(enabled=True, forward_prefetch=True, execution_trace_complete=True,
                    mixed_precision=MixedPrecisionConfig(enabled=True, dtype='float16')))
out = apply_candidate(cfg, PlanCandidate(4, 2, 1, 'fsdp', 0.0, ('test',)))
print('sp_backend', out.parallel.sp_backend)                # 'none'   ← 序列并行被静默关闭
print('mp.enabled', out.fsdp.mixed_precision.enabled)       # False    ← 混合精度被静默关闭
print('mp.dtype',   out.fsdp.mixed_precision.dtype)         # 'bfloat16' ← dtype 被改
out.validate(world_size=8); print('validate passed')        # 通过 → 完全静默
"
```

实测输出：

```
BEFORE: sp_backend='ulysses' | fwd_prefetch=True trace_complete=True mp.enabled=True mp.dtype='float16'
AFTER : sp_backend='none'    | fwd_prefetch=False trace_complete=False mp.enabled=False mp.dtype='bfloat16'
        ... 且 AFTER 通过 validate(world_size=8)
```

该 candidate 的 reason 字符串偏偏写着 `"preserved explicit parallel config"`。**违反项目自己在 `config.py:135` 确立的 "refusing silent disable" 惯例。**

### 3.4 `suggest_plan()` 永远无法返回启用的计划

**证据链**（`planner.py:104` 括号位置错误）：

```python
explicit_non_default = any((explicit.dp_size, explicit.tp_size, explicit.pp_size) != (1, 1, 1)
                           or explicit.sp_backend != "none" or explicit.pp_schedule != "none")
```

`any()` 收到的是整个 `(...) != (...) or ... or ...` 求值后的**单个 bool**，而非可迭代对象。

**复现步骤**（✅实测）：

```bash
$PY -c "
from aitrainer.config import FrameworkConfig, PlanningConfig
from aitrainer.planner import suggest_plan
class L: 
    def named_children(self): return iter(())
    def parameters(self): return iter(())
class M:
    def __init__(self): self._l=[L()]
    def children(self): return iter(self._l)
suggest_plan(M(), config=FrameworkConfig(planning=PlanningConfig(enabled=True)), world_size=1)
"
# TypeError: 'bool' object is not iterable
```

叠加第二条：`world_size=4` 且不显式传 `Topology` 时，`planner.py:99` 先用默认 `make_rank_mapping(world)`（pp=dp=tp=1）→ `TopologyError: mesh product=1 does not match world_size=4`（✅实测）。

**结论：不存在任何能让启用状态的 planner 返回报告的非空输入。** 而唯一的测试（`tests/unit/test_planner.py:13`）用 `planning.enabled=False`，在 `:96` 提前返回 —— 所以 CI 绿。同时 `capability_matrix()` 把 `planning_report` 标为 `STABLE`，README Batch 8 称交付了 "deterministic topology/stage planning reports"。

### 3.5 梯度桶 reducer 一生只归约一次

**证据链**（`src/aitrainer/overlap/gradient.py`）：`:48-49` 的 `if self._finished: return` 配合 `:63` 的 `self._finished = True`，而能清除它的 `reset()`（`:64`）**全仓库无调用方**（`GradientBucket`/`GradientBucketReducer` 只出现在 `overlap/`、`api.py` 与测试中）。

**复现步骤**（✅实测）：

```bash
$PY -c "
from aitrainer.overlap.gradient import GradientBucketReducer
class T:
    def numel(self): return 8
    def element_size(self): return 4
calls=[]
r = GradientBucketReducer(1024, accumulation_steps=1, reduce_fn=lambda v: calls.append(len(v)))
for _ in range(5):
    r.register('w', T()); r.mark_microbatch_end(); r.finish_grad_sync()
print('reduce_fn called', len(calls), 'time(s); expected 5')
print('stranded tensors in buckets:', sum(len(b.tensors) for b in r.buckets))
"
```

实测输出：

```
reduce_fn called 1 time(s); expected 5
stranded tensors in buckets: 4
```

`accumulation_steps=4` + 12 个 microbatch 时同样只归约 **1 次**（应 3 次），滞留 8 个张量。

**影响**：**多卡场景下 rank 在第一个窗口之后就不再 all-reduce，静默发散**；且残留张量无界增长。`accumulation_steps` 参数与 `mark_microbatch_end()` 在首次同步后全部沦为装饰。

### 3.6 静态 P2P 接收方从错误的对端取 payload

**证据链**（`src/aitrainer/parallel/pp_p2p.py`）：`_peer(True)` = 下游（`send_forward` 的 `dst`，`:106`），`_peer(False)` = 上游（`send_backward` 的 `dst`，`:142`）。

| 方法 | 读 header 的对端 | 读 payload 的对端 | |
|---|---|---|---|
| `recv_forward`（`:84-86`） | `_peer(not True)` = **上游** | `src=self._peer(True)` = **下游** | ❌ 矛盾 |
| `recv_backward`（`:120-122`） | `_peer(not False)` = **下游** | `src=self._peer(False)` = **上游** | ❌ 矛盾 |
| `recv_forward_dynamic`（`:95`） | 上游（`:309` `source = self._peer(False)`） | `_peer(False)` = 上游 | ✅ |
| `recv_backward_dynamic`（`:131`） | 下游（`:309`） | `_peer(True)` = 下游 | ✅ |

两个**静态**接收方读 header 与读 payload 用的是**相反**的对端；同一文件的**动态**版本两者一致 —— 正确写法就在下面 9 行。

**影响范围（重要）**：训练器实际管线路径（`PipelineStage.pipeline_step` / `pipeline_evaluate`）**只使用动态 API**，因此 `Trainer` 不受影响。受影响的是 README 宣传为 Batch 4 交付物、并被导出的 `GPipeSchedule` / `OneFOneBSchedule`，以及外部调用方。🔍**分审计**用进程内多 rank 模拟器复现：`pp_size=2` 时末级抛 `P2PError: last pipeline stage has no forward peer`，首级永久阻塞 → 挂死。

### 3.7 `Trainer.evaluate()` 在 PP 下返回 NaN，且无跨 rank 归约

**证据链**：

```
distributed_model.py:308   return {"loss": torch.stack(losses).mean() if losses else None, ...}
                           # :300 只有 is_last 分支才 append 到 losses → 首/中间级 losses 恒空 → loss=None
trainer.py:313-315         if torch.is_tensor(value): losses.append(...)
                           continue            # torch.is_tensor(None) == False → 该 rank 不贡献任何值
trainer.py:321             "loss": sum(losses)/len(losses) if losses else float("nan")
```

→ 任何 `pp_size > 1` 的运行，**除最后一级外每个 rank 的 `evaluate()` 返回 `{"loss": nan, "steps": 0.0}`**，没有任何代码把最后一级的 loss 广播出去。

**次生问题**：`trainer.py:304` 用 `data.train_dataloader(dp_group=None, ...)` —— 没有独立 eval loader；`:321` 只做本地平均、**无 `all_reduce`**。而 `DataProviderAdapter.train_dataloader`（`data.py:105-106`）把 `dp_group=None` 原样传给**用户自己的 `loader_factory`**，所以 eval 的分布式语义取决于用户工厂的未文档化行为。

**第三个问题**（🔍分审计从另一角度发现）：`pipeline_evaluate`（`distributed_model.py:290-292`）对 Mapping 类型 batch 是**按位置**传递（`self.module(value)`），而 `pipeline_step`（`:129-135`）是 `**values` 解包 —— 于是 `{"input_ids":..., "attention_mask":...}` 这类 batch 在 evaluate 里会把整个 dict 当 `input_ids` 传进去。

### 3.8 `seed` 无法让权重初始化可复现

**证据链**：`seed_everything` 在 `Runtime.__init__` 内调用（`runtime.py:70`），而 Runtime 在**模型构造之后**才创建：

| 位置 | 动作 |
|---|---|
| `trainer.py:107` | `model = model_or_adapter.build(...)` ← 权重在此随机初始化 |
| `trainer.py:113` | `runtime_obj = Runtime(..., seed=config.seed)` ← 种子在此才设 |
| `trainer.py:59` | 直接构造 `Trainer` 时同理：用户先建模型，再进 `__init__` 设种子 |

**影响**：README Batch 0 称 "reproducible single process trainer"，`examples/tiny_transformer/train.py` 特意传 `seed=123` —— 但**每次运行初始权重都不同**，同种子两次运行无法对比。另外 `seed_everything`（`runtime.py:32-42`）只 seed `random` + torch + torch.cuda，**没有 numpy**。

### 3.9 `dry_run()` 无法校验任何多卡配置（CLI 可见）

**证据链**：`diagnostics.py:77` 的 `dry_run(config)` **没有 `world_size` 参数**，直接调 `validate(config)`（默认 `world_size=1`）。

**复现步骤**（✅实测）：

```bash
printf '{"parallel": {"dp_size": 8}, "fsdp": {"enabled": true}}' > /tmp/f_multi.json
$PY -c "from aitrainer.cli import main; main(['dry-run','--config','/tmp/f_multi.json'])"
#   aitrainer: error: parallel dimensions product=8 does not match world_size=1
$PY -c "from aitrainer.cli import main; main(['validate','--config','/tmp/f_multi.json','--world-size','8'])"
#   {"status": "ok"}
```

一个合法的分布式配置，`validate` 通过、`dry-run` 报假错误。README 把 `dry-run` 作为常规命令列出。

---

## 4. P1 缺陷

| # | 缺陷 | 位置 | 影响 | 验证 |
|---|---|---|---|---|
| 4.1 | `no_sync()` **定义了但从不被调用** | `trainer.py:61` 只有 `hasattr` 守卫；定义在 `distributed_model.py:34,70`、`parallel/fsdp.py:88`；`Trainer` 从不进入 `self.model.no_sync()` | FSDP/DDP + 梯度累积时每个 microbatch 都做完整归约（性能）。**且守卫测的是原始入参**，而 `DistributedModel.no_sync` 在内部模块无 `no_sync` 时返回 `nullcontext()`（`:34-35`）→ 传 `DistributedModel(plain_module)` 可**通过守卫但零梯度同步**，各 rank 副本静默发散 | 📖 |
| 4.2 | TP-only 无法用 `Trainer` 训练 | `parallelize` 在 `pp_size==1` 时 `return stage`（`parallelizer.py:89`）返回裸模块；`nn.Module` 无 `no_sync` → `trainer.py:61` 抛 *"multi-rank training requires an FSDP-wrapped model in Batch 1"*，且错误信息**误诊为"需要 FSDP"** | `tp` 被标为 `STABLE`、`ConfigPreset.tp()` 存在，但框架自己的 `Trainer` 训练不了 TP 模型（只能手写循环，如 `examples/tp_train`） | 📖 |
| 4.3 | `_enforce_budget` 上限失效 | `overlap/controller.py:29-33` 等待 `pending[0]` 但**从不 release**；`wait()`（`:44-47`）也不 release；`ExecutionScheduler.pending` 只排除 `RELEASED` | 被等待的 op 永远留在 `pending`，后续每次都在 wait 一个已 WAITED 的 op（立即返回）→ 两个预算在首次突发后**永久失效**。✅实测：`max_inflight_ops=2` 下 6 轮 submit+wait 后 `pending=6`，状态全为 `WAITED` | ✅ |
| 4.4 | `mesh.py` 吞掉 `ProcessGroupError` → 静默退化为 world group | `mesh.py` 的 `except (ImportError, RuntimeError, TypeError)` 捕获 `ProcessGroups.create` 抛出的 `ProcessGroupError`（它是 `RuntimeError` 子类，`parallel/groups.py:11`）→ `_groups` 保持 `None` → `tensor_parallel_group` 返回 `None` → `parallelizer.py:74` 传给 TP 层 → 而 `dist.get_world_size(None)`/`all_reduce(group=None)` 意味着**整个 world** | `tp_size=2, pp_size=2` 下若 `new_group` 失败，TP 层按 world=4 切权重、在属于不同 PP stage 的 rank 间 all-reduce。**这正是项目自己第 3 条原则禁止的静默降级**（`parallel/fsdp.py:131-134` 是唯一会复查的路径） | 📖 |
| 4.5 | `fit()` 收尾 flush 缺 `grad_clip_norm`；`max_steps` 残留梯度 | 累积边界块 `trainer.py:236-241` 有裁剪，收尾 flush 块 `:279-299` 有 `unscale_`/`step`/`zero_grad`/`scheduler.step` **唯独没有裁剪**；`:277-278` 在 `max_steps` 时提前 `return` 而 `.grad` 未清零；`:220` 恒除**完整**的 `grad_accumulation_steps` 使 flush 梯度缩放不匹配 | `grad_clip_norm=1.0` + 剩余 microbatch 时最后一次更新未裁剪（可能任意大/NaN）；下次 `fit()` 在旧梯度上继续累积。**单卡即触发** | 📖 |
| 4.6 | GradScaler 跳过更新时 `optimizer_step`/`scheduler` 仍前进 | `trainer.py:242-252`：`scaler.step()` 返回 `None`（跳过）未被检查，`:251` `scheduler.step()` 与 `:252` `optimizer_step += 1` 无条件执行，`StepOutput.optimizer_step=True` 照报 | fp16 下一次 loss 尖峰就让 LR 调度多走一格，checkpoint 记录虚高的 `optimizer_step` | 📖 |
| 4.7 | checkpoint 层四处缺陷 | ① `verify_checksum`（`checkpoint/format.py:20`，默认 `True`）**从不被读取**（grep 全仓库只有定义处）；`ShardSpec.checksum`、`tensor_schema_hash`、`model_config_hash` 只写不校验 → manifest 加载路径只比对 shape（`reader.py:119`） | 损坏但形状对得上的分片**静默加载错误权重** | 📖 |
| 4.7b | 跨布局守卫为空 | `reader.py:86` 的 `world_size: int \| None = None` 默认取 `manifest.world_size_at_save` → `:87` 比较**恒为假**；且只比**总数**，从不比 `logical_sharding`；`:86-98` 的 `for shard in selected:` **无 `else` 分支** | `tp=2,dp=1` 存的 checkpoint 能被 `tp=1,dp=2` 接受；匹配不到 shard 的张量被**静默跳过**，模型带随机初始化继续训练，无异常无警告 | 📖 |
| 4.7c | converter 的 PP 维度根本没切分 | `checkpoint/converter.py:99-111`：`pp_rank` 循环体内是 `chunk = chunks[tp_rank]`，**每个 pp_rank 拿到同一个 chunk**；pp_rank 只影响文件名与记录的目标 rank。而 manifest 却声明 `logical_sharding={"pp": pp_size}` | 每个 PP stage 的文件里都是**完整** dense state dict（体积 ×pp_size）；把 stage-local 模型加载进去会在 `reader.py:117` 抛 `KeyError`。**DP 复制是正确的，PP 完全没做** | 📖 |
| 4.7d | `save_dcp` 第二次保存必失败 | `checkpoint/manager.py:196` 直接 `os.replace(temporary, target)`，无 `save()`（`:115-127`）那套"先把旧的挪开"的动作 | ✅实测 POSIX 语义：`os.replace(dir → 非空目录)` → `OSError 66 Directory not empty`。第二次 `save_dcp` 到同一路径抛裸 `OSError`，而 `except` 分支（`:198-201`）随即 `shutil.rmtree(temporary)` **把刚写好的新 checkpoint 删掉**。仓库内无调用方，但它是公开 API | ✅ |
| 4.8 | `MixedPrecisionConfig.dtype` 零校验 | `PrecisionConfig` 有 5 个 dtype 的白名单（`config.py:56-61`），但嵌在 `FSDPConfig` 里的 `MixedPrecisionConfig` 既无 `validate()` 也从不被 `FSDPConfig.validate()`（`:94-106`）检查 | ✅实测：`dtype="float64"` 甚至 `dtype=object()` 都通过，`aitrainer validate` 返回 `{"status":"ok"}` —— **专用于校验配置的命令给非法 dtype 开绿灯** | ✅ |
| 4.9 | `from_dict` 的未知字段检查只做一层 | 顶层检查干净（`config.py:251-254`），嵌套层直接 `typ(**raw)`（`:260-267`） | ✅实测：顶层笔误报干净的 `ConfigurationError`；嵌套笔误抛 `TypeError: ParallelConfig.__init__() got an unexpected keyword argument 'tp_sizee'`（泄漏内部类名）；`{"parallel": null}` 抛 `AttributeError: 'NoneType' object has no attribute 'dp_size'`。CLI 的 `except Exception`（`cli.py:43`）兜住了不崩栈 | ✅ |

### 4.10 🔍分审计报告、未独立复跑的条目

以下条目由分块审计智能体用**真实的 2/4 进程 gloo 测试装置**得出（其 `/tmp` 产物含真实的 `ProcessGroupGloo.cpp:547` 警告与 4-rank 建组日志，佐证执行真实性），但**审计者未逐条复跑**，使用前建议独立验证。**编号从 b 起：原 (a) `ProcessGroups.create` 命名空间冲突已升级为 §2.4**（置信度 CERTAIN、严重度 critical，审计者已核验其 A/B 产物与 gloo 超时日志）。

| # | 缺陷 | 位置 |
|---|---|---|
| b | `ColumnParallelLinear` 在 `gather_output=True` + bias 时，把收集后的完整激活与**分片**偏置相加 → 形状不匹配（实测 `RuntimeError: tensor a (6) must match tensor b (3)`）。`skip_bias_add=True` 返回同样不一致的 `(full_output, sharded_bias)` 对 | `parallel/tp.py:82-86` |
| c | `SequenceParallelLayerNorm` 前向正确，但**TP 组内无参数梯度归约** → 复制的 `norm.weight`/`norm.bias` 在各 TP rank 上发散（实测：dense `dW abs-sum=127.9986` vs 各 rank `63.9993`；加一次 all-reduce 后误差 1.9e-06） | `parallel/sp_sequence.py:41-44` |
| d | `distributed_attention` 的 `seq_lens` 路径在 `world>=2` **必崩**：掩码按交换前（逐 rank）长度构建，却用于 head/seq 交换后的张量 | `parallel/sp_ulysses.py:98-103` |
| e | 静态 `GPipeSchedule` 的反向发送顺序为**升序**，而接收端（`pp_schedule.py:172-173`、`:138-139`）期望**降序** → `P2PError: pipeline microbatch metadata mismatch`（`num_microbatches>=2`） | `pp_schedule.py:155` vs `:172` |
| f | `OneFOneBSchedule` 在 `num_microbatches > pp_size` 时**三方死锁**（`:199` 只发 `pp_size` 个前向就收反向，而中间级 `:237-242` 要消费完全部 M 个）。而 `config.py:42-43` **强制要求** `1f1b` 时 `M >= pp_size`，即官方推荐区间正好落在死锁区。`M == pp_size` 时改为触发上面 e | `pp_schedule.py:193-207, 237-242` |
| g | 反向的 `stage` 元数据在中间级差 1（发送方由**收到的** spec 推导，接收方由**另一个**张量推导）→ `pp_size>=3` 必错 | `pp_schedule.py:139,173,245` |
| h | `ActivationOffloader` 的 pack/unpack 钩子**不幂等**，违反 PyTorch 契约（unpack 可被多次调用且须返回相同数据）：第二次 unpack 返回 `None` | `offload/activation.py:98, 111-113` |
| i | `drain()` 把缓冲还给池但**不清空** `record.cpu` → 之后的 unpack 读到已释放的存储并**二次 release**；若期间缓冲被重新租出，错误释放会被接受，同一缓冲被交给两个消费者（**静默激活值损坏**） | `offload/activation.py:152-158` |
| j | `ParameterPrefetchCoordinator` / `PingPongBuffer` 的若干边界问题（详见分审计原文） | `overlap/` |
| k | `Profiler.summary()["overlap_ratio"]` 分子分母量纲不同（分子是 `record_wait` 的 `overlapped_seconds`，分母是 `section()` 时长之和）→ 实测可达 `1.13e7`；嵌套 `section()` 会在分母里重复计数 | `profiler.py:94-109` |
| l | `lifecycle.py`：`wait(timeout=...)` 的 deadline 只在进入阻塞调用**之前**检查 → 一个挂住的 NCCL 集合通信会让 `drain()` 永久阻塞而非抛 `TimeoutError`；且超时/失败的 op 经 `release()` 后再次 `wait()` 会**静默返回 `None`**（失败变成成功） | `lifecycle.py:73, 81-83, 89-96` |
| m | `KernelBackend.register()` 对 `backend == "eager"` 无条件抛错，而 `attention_capability()` 在无 flash SDPA 的机器上正是返回 `backend="eager"` → **回退能力在最需要它的机器上无法注册** | `kernels/backend.py:60`, `kernels/attention.py:19-27` |
| n | `overlap/metrics.py:6-8` 的 `prefetch_hit/miss/late/evicted` **无任何写入方**；`controller.py:55` 的 `inflight_peak` 是 `float(len(self.records))`（累计提交总数，非并发峰值） | `overlap/metrics.py`, `overlap/controller.py:55` |
| o | `Linear`/`AdaptiveAvgPool` 等：`fused_adamw_step` 与 `AdamW` 代数等价、`rms_norm` 的 eps 位置与 `fused_mlp` 激活顺序正确；但 `rms_norm` 将 fp32 的 `rsqrt` 因子**先降精度再相乘**，对 bf16/fp16 输入相对参考实现有 ~0.2-0.4% 偏差，与 `backend.py:93` 声明的 `error_budget=0.0`（"numerically transparent"）不符 | `kernels/fused.py` |

---

## 5. P2：死代码与文档漂移

### 5.1 被标为 `STABLE` 但无任何调用方

`capability_matrix()`（`capability.py`，41 项）的 `STABLE` 标签是给用户的承诺。以下四项经 grep 确认**只存在于定义处 + `api.py` 再导出**，`src/` 无任何代码路径消费：

| 能力 | 声称 | 实测 |
|---|---|---|
| `Profiler`（README："Timeline、通信 wait/hidden 时间、pipeline bubble、GPU idle… 可从 `Profiler` 获得"） | 指标可用 | `grep "Profiler("` 在 `src/`、`examples/`、`scripts/` 中**零命中**，只有 3 个测试文件。用户跑完一轮什么也拿不到 |
| `kernel_eager_fallback` | "带能力记录的 eager kernel" | `KernelBackend` 只在 `kernels/backend.py`、`kernels/__init__.py`、`api.py` 出现；`kernels/attention.py` 与 `kernels/fused.py` **根本不使用这个注册表** |
| `planning_report`（README Batch 8："deterministic topology/stage planning reports"） | 规划报告 | `suggest_plan`/`apply_candidate`/`PlanReport` 只在 `planner.py` + `api.py` + 一个测试；CLI 的 `dry-run` 返回 config + capabilities，**不含 plan** |
| `ModelAdapter` / `OptimizerFactory`（README Batch 0："adapter protocols"） | 适配器协议 | 两个 Protocol 从未被任何函数作为参数类型接受（`DataProvider`/`LossFn` 真被用了 —— `trainer.py:54,97,260,302`） |

> 项目自己 `docs/开发方案与开发规划.md` §9.3.1 把"公共方法存在但没有调用方，或配置项存在但运行时未消费"**定义为遗留项**。上述四项均符合该定义，却标为 `STABLE`。

### 5.2 配置字段/度量恒为默认值

| 项 | 位置 | 问题 |
|---|---|---|
| `PrecisionConfig.param_dtype` | `config.py:48` | 只出现在定义处与 `presets.py`；真正生效的是 `FSDPConfig.mixed_precision.dtype`（另一个字段，`parallel/fsdp.py:24-32` 读它）→ **`param_dtype` 无消费者** |
| ~~`PrecisionConfig.reduce_dtype`~~ | `config.py:51` | **曾修好，又被迁移推翻，最终删除**。`replace_linear_modules` 增加 `reduce_dtype` 参数并由 `parallelizer.py` 透传到 `RowParallelLinear` 的归约——那是真的接线。DTensor 迁移（`773107d`）把整层删掉换成 `RowwiseParallel`（该 style 不接受 dtype），**接线随之消失，而字段、它的校验、设置它的两个 preset 和声称「threaded into the TP row projections」的 docstring 全部留下**。于是它又「看起来是接的」活了几轮。见「死配置字段的清零」一节 |
| `overlap_ratio` | `overlap/controller.py:11,52` | `OverlapRecord.overlapped_seconds` **全仓库从未被赋值** → `summary()["overlap_ratio"]` 结构性恒为 `0.0` |
| `ConfigPreset.memory_saving()` / `performance()` | `presets.py:29-38` | 两个函数体**逐字节相同**（都是 `PrecisionConfig(bfloat16/bfloat16/float32/bfloat16/float32)`）。用户用这两个预设做性能对比会发现"毫无差别" |

> **讽刺的对称**：真正生效的 `mixed_precision.dtype` 是唯一**没有被校验**的 dtype 字段（§4.8）；而被校验的 `param_dtype`/`reduce_dtype` 是**死字段**。

### 5.3 其他漂移

| 项 | 位置 | 说明 |
|---|---|---|
| `torch.compile` 拒绝信息过期且三处矛盾 | `config.py:75`（"unsupported in **Batch 4**"）、`capability.py:120`（"Batch 4"）、`capability.py:104`（"not enabled in **Batch 1**"） | 仓库自称已完成到 Batch 9；而 `docs/设计文档.md` 把"可选 torch.compile"列为**第一阶段能力** |
| `examples/offload_train/train.py` 缺 `sys.path` hack | 其余 6 个示例都有（`tiny_transformer:8`、`fsdp_train:13`、`tp_train:12`、`tp_sp_train:8`、`pp_train:8`、`adapters_train:6`） | ✅实测：裸检出跑 `python examples/offload_train/train.py` → `ModuleNotFoundError: No module named 'aitrainer'`（其余 6 个不需要 `pip install -e .`） |
| 全仓库零个示例配置 JSON | `find . -name "*.json" -not -path "./references/*"` → **0** | `aitrainer validate --config config.json` 是文档主推命令，`_load_config` 又只吃 JSON（`cli.py:14-22`）→ 用户第一步就卡住；无 schema、无示例 |
| `capability.py:139-143` 是空操作 | `capability.py:139-143` | 该 `if`（bfloat16 + CPU）是函数**最后一条语句**，`return` 等价于 `pass` —— 注释描述了一个决策，代码没有任何效果。无害，但说明检查未写完 |
| `offload/parameter.py:97-102` 死分支 | `offload/parameter.py:97-102` | `prefetch()` 的两个分支**逐字节相同**（都 `return self.fetch(...)`），注释声称的 "trace 必须显式 finalize" 门控没有任何效果 |
| `rank_state.pt` 单一共享路径 | `checkpoint/manager.py:87-127, 173-177` | 每个 rank 各自 `mkdtemp` 但均写 `rank_state.pt` 并发布到**同一** `target`（`os.replace`，最后写入者胜），resume 时所有 rank 恢复**同一个** RNG 状态 → 各 rank 重放相同 RNG 流，破坏 `runtime.py:70` 按 rank 加种子的初衷（dropout 掩码在所有 rank 上相同） |
| `save_dcp` / 目录发布非崩溃持久 | `checkpoint/manager.py:108-114` | `checksums.json` **从不 fsync**（只有 `rank_state.pt`/`metadata.json`/`READY` 有）；目录级 fsync 完全缺失 → 断电后可能出现 `READY` 已持久而 `checksums.json` 未持久，而 `_verify` 把"文件不存在"当作"无需校验"（`:143`）→ 加载**未校验**的 checkpoint。另 `torch.load(..., weights_only=False)`（`:164`）意味着篡改的 checkpoint 可执行任意 pickle |

---

## 6. 已证伪（这些**不是**缺陷，避免误报）

审计过程中有多条假设被推翻，记录于此以免后续重复劳动：

| 曾怀疑 | 实际结论 |
|---|---|
| `FrameworkConfig.replace()` 绕过校验 | 冻结 dataclass 确实无 `__post_init__`，但**所有入口都会重新校验**：`trainer.py:60`、`capability.py:111`、`offload/manager.py:27`、`checkpoint/reader.py:14`、`plugins/models.py:43` |
| `GPipeSchedule.run()` 在非首级抛错是缺陷 | 报错信息明确（"requires run_last_stage with input specs"），且 `run_last_stage`/`run_middle_stage` 确实存在（`pp_schedule.py:145,161,211,230`）—— 这是**响亮失败**的 API 分工，最多算 README 未说清 |
| 生产路径的 GPipe/1F1B 顺序错乱（FIFO/LIFO 错配） | 🔍模拟 `PipelineStage.pipeline_step` 于 pp=2/3/4、M=4/6/8：每 rank 恰好 M 次前向 M 次反向，每个 microbatch 恰好反向一次，每个激活配对**自己**那个 microbatch 的梯度，无挂起。顺序缺陷只在静态 `pp_schedule.py` 路径 |
| 损失漏除以 microbatch 数（因子 M 错误） | 不存在。`distributed_model.py:166,232` 与 `pp_schedule.py:79,154,223` 都除了一次 `len(batches)`，报告时乘回 |
| `split_sequential` 把 head 放到 stage 0 | 不存在。`pp_shapes.py:98-105` 用 `children()` 顺序 → embedding 留在 stage 0、head/last-norm 留在最后一级，参数绑定保留，无模块丢弃或重复 |
| `plan_stages` 在层数非整除时产生空 stage | 健全。拒绝 `pp_size > len(layers)`；非整除时边界 `round(stage*len/pp_size)` 严格递增 |
| `PingPongBuffer` 有 ABA 复用问题 | 不存在。单调递增的 per-index generation **确实**防住了 ABA；`release()` 拒绝释放被在途 op 占用的槽位 |
| `Backpressure` 有 `_enforce_budget` 那个 bug | 不存在。`drain_oldest` **会** `pop`，计数限制持续生效 |
| optimizer state 的 `step` 漏移 / bias correction 错误 | 不存在。`Adam/AdamW` 的 `step` 是 float32 张量，`_map_tensors` 的浮点过滤会移动它（`offload/optimizer.py:21,105-121`） |
| `ParameterOffloader` 的 release 用陈旧 master 覆盖 GPU 更新 | 该 bug **不存在**（`fetch` = CPU master → device；`release` = device → `master.copy_()` 后 `param.data = master`，方向正确）。只是被 §3.2 的 `KeyError` 挡在前面永远走不到 |
| `_CopyToTP` / `_all_to_all_impl` 的 autograd 有误 | 🔍数值验证正确：`_CopyToTP` 单位前向 / all-reduce 反向（= Megatron `_CopyToModelParallelRegion`）；Ulysses 交换是精确对合且其逆即转置；`distributed_attention`（不含 `seq_lens`）与稠密 SDPA **完全一致，含反向**（`attn_err=0.0`） |
| `Column→Row` 对与稠密对不等价 | 🔍验证等价（前向最大误差 6e-08，各梯度 ≤1.2e-07，fp32 噪声）；`from_dense` 的切片偏移正确，`_require_divisible` 被强制执行 |
| `pad_sequence`/`unpad_sequence` 在无需填充时丢真实 token | 不存在。往返完整，`pad_nopad_identical=True`，逐样本长度 `[4,2]` 被保留 |
| `apply_rope` 的 position offset 有 off-by-one | 与参考交错 RoPE **完全一致**（`err=0.0`），`position_offset=k` 等价于用 `cos[k:]` 调用 |
| `fused_adamw_step` / `rms_norm` / `fused_mlp` 公式错 | 代数等价（`rms_norm` 的 eps 位置、`fused_mlp` 的激活顺序、`AdamW` 的 bias correction 均正确）。仅有一处精度细节见 §4.10(o) |
| `topology.make_rank_mapping` 的 rank 顺序错 | ✅实测正确。10 组 `(dp,tp,pp)` 组合全部为**完美双射**（`distinct == world` 且覆盖所有坐标），tp 变化最快、其次 dp、最后 pp；正确拒绝 `product != world_size` 与非正数维度。`mesh.py` 的 `init_device_mesh(..., (pp,dp,tp))` 与之一致 |
| "配置工具链不依赖 torch" 只是声称 | ✅实测成立。无 torch 时 `import aitrainer`（183 个导出名）、`capability_matrix()`（41 项）、整个 `aitrainer` CLI 均正常工作；`inspect-topology` 输出完整 JSON。这是本仓库真实且做得不错的一处 |

---

## 7. 可用性缺口：为了真实可用还缺什么

### 7.1 阻塞级（不解决则无法用于任何真实任务）

1. **TP/SP 全崩（§2.1）** —— 修 `forward` 参数数（3 处）+ `reduce_dtype` 作用域，然后**把测试跑起来**。当前最高的单项。
2. **元组 batch 契约破损（§2.2）** —— `train_step((x, y))` 是文档主推用法，却要求模型的 `forward` 必须恰好接受 2 个位置参数。修复方向：`self.model(*batch)` 应只传输入，或在契约中明确要求模型签名。
3. **数据层不存在** —— 全仓库零个 `Dataset`/`DataLoader`/`tokenizer`/`from_pretrained`（grep 验证），所有示例都是 `torch.randn`/`randint`。`data.py` 是契约外壳：`DataProviderAdapter`（`:89-114`）**不构造** `DataLoader`，只调用用户传入的 `loader_factory(dp_group=..., seed=..., epoch=...)`。**没有任何从磁盘数据到训练的路径。**
4. **配置示例 + schema** —— `validate --config config.json` 是用户第一个会试的命令，而仓库里没有任何 JSON，也没有 schema 表。
5. **`evaluate()` 的分布式语义（§3.7）** —— 无跨 rank 归约、复用训练集 loader、PP 下返回 NaN。**静默错误的指标比没有指标更糟。**
6. **权重初始化不可复现（§3.8）** —— `seed` 在模型构造之后才生效。
7. **示例可运行性** —— ✅实测 7 个示例中 3 个崩溃（`tp_train`、`pp_train`、`offload_train`），`fsdp_train` 需 torchrun，只有 `tiny_transformer` 真正训练。
8. **多卡（≥3 rank）作业死锁（§2.4）** —— 组创建阶段的 store 命名空间冲突让 ≥3 rank 的作业在启动时挂死 30 分钟后报错。`world_size<=2` 安全，这正是"只在 2 卡上试过"会漏掉它的原因。修 `groups.py:42-45` 是**多卡可用的前置条件**。

### 7.2 其次

8. **许可证与 provenance** —— 无 `LICENSE`；`references/uylesses` 是阿里 Wan2.1 的代码拷贝（5 文件 / 320 行，每文件带 `Copyright 2024-2025 The Alibaba Wan Team`），**但没有 LICENSE 文件** —— 而 `docs/设计文档.md` §14 自己要求"实际移植必须保留 NOTICE、许可证审查记录和版本锁定信息"。且该目录作为包无法导入（`from ..modules.attention import ...` 的上级包不存在），是功能切片而非完整仓库。（对照：`references/DeepSpeed` 495M 完整 clone v0.19.5-46，`references/Megatron-LM` 196M 完整 clone core_v0.15.0rc7，二者纯作机制参考，并由 `scripts/check_no_reference_imports.py` 的 AST 检查**机械保证**不被 import。）
9. **`git init` 并让 CI 真的跑** —— 当前 CI 若执行会红 10 个测试；且因为无 `.git`，它从未执行过。需补 GPU/torchrun job。
10. **英文文档** —— 两份设计文档均为中文；无 API reference、无 `CONTRIBUTING`、无 `CHANGELOG`、无版本号（README 用 "Batch" 计数）。
11. **Profiler 接线或降级标签**；修正 `kernel_eager_fallback`/`planning_report`/`overlap_metrics` 三个不可达的 `STABLE` 标签。
12. **checkpoint 硬化（§4.7）** —— 真正校验 checksum、比对 `logical_sharding` 而非只比总数、`selected` 为空时报错、converter 切分 PP。
13. **真实预训练权重加载** —— `Llama/GPT/BERT` 适配器经 `_build()` 用 `nn.TransformerEncoderLayer` **随机初始化**（模块 docstring 诚实地说了，README 未标注）；`CheckpointConverter`（`converter.py`，约 600 行）与 `ModelLoader` 是完整实现但**除测试外无任何调用方**。
14. **推理/导出** —— 完全没有（无 `predict`、无 ONNX/TorchScript、无生成循环、无 tokenizer）。纯训练框架。

### 7.3 最小可行路径（按依赖顺序）

1. 修 TP/SP `forward` 参数数（§2.1）+ `reduce_dtype` 作用域
2. 修元组 batch 契约（§2.2）
3. 让 `pytest` 在**装了 torch** 的 CI 里变绿
4. 提供一个真实数据集的 worked example（文件 → `DataProviderAdapter` → 训练）
5. 补 `examples/config.json` 并让 `aitrainer validate --config examples/config.json --world-size 1` 通过
6. 修 `evaluate()` 的跨 rank 归约与独立 eval loader
7. 把 `seed` 提到模型构造之前

**第 1-3 项做完，"待验证"这个状态才有意义。**

---

## 8. 审计局限（必读）

1. **多卡 `torchrun` 路径未能在本机验证。** README 的 `torchrun --standalone --nproc_per_node=2 examples/fsdp_train/train.py` 在本机挂起，日志只有 c10d 的 `socket.cpp:767 ... gai error: 8 - nodename nor servname provided`（`Huawei-MateBook.local` 的 IPv6 解析失败），**没有任何 aitrainer 栈帧**；加 `--master_addr=127.0.0.1` + `GLOO_SOCKET_IFNAME=lo0` 后仍挂起。分审计亦报告"用纯 torch（无 aitrainer 代码）时 `init_device_mesh` 同样挂起"。故记为**环境阻塞、结论不可得**，而非框架缺陷。真实的多卡验证仍需一台能起 rendezvous 的服务器：
   ```bash
   bash scripts/run_all_tests.sh --world-size 2 --report-dir artifacts/server_report
   # --world-size 4 才能跑通 tp=2,pp=2 那条组合入口
   ```
2. **§4.10 的条目未经审计者独立复跑**，仅由分审计智能体自证。使用前建议独立验证。
3. 本报告**未**审计 `references/` 下的三个第三方仓库内容（仅确认其角色与 provenance）。
4. 所有本文档引用的行号基于审计时的仓库状态；该仓库不是 git 仓库，无 commit 可用于锚定版本。

---

## 9. 总评

这个仓库的**工程纪律是真诚的**：能力分级、fail-fast 校验、原子 checkpoint 发布、禁止 import 参考仓库的 AST 守卫、"未验证不得标记完成"的规范、README 主动标注 Batch 1 待验证。这些比许多成熟项目做得好。

但代码的真实状态与文档的差距比预期**大一个数量级**：不是"边缘功能未接线"，而是 **TP 与 SP 两个核心能力在任何 world size 下都抛异常**，且 **仓库自己的测试套件在装上 torch 后立刻红 10 个**（其中 5 个由框架自身的 batch 契约缺陷导致）。这解释了一切 —— 这套 6.4k 行代码是在**没有 torch 的机器上**写出来的，所有分布式能力从未被执行过一次。`待验证` 标签是诚实的，但它覆盖的范围比作者以为的广得多。

一条元教训：`pytest.importorskip("torch")` + 无 torch 的开发环境 = **一套静默全绿的测试**。CI 存在、断言存在、纪律存在，但唯一的信号通道是断的。

## 模型解耦：SP 的静默空转、PP 的切分注入点、RMSNorm（2026-09-17，已修）

起因是一个评估问题：**拿自己的模型来用，要改多少模型代码？**逐轴查下来，FSDP / 单卡 /
检查点是零侵入，TP 是命名耦合但会拒绝，SP 与 PP 各有一个真实缺陷。

### 一、SP 对不认识的 norm 类型静默空转

`sequence_parallel_styles` 按**结构**匹配（`torch.nn.LayerNorm`），这是零命名耦合的来源，
代价是别的 norm 类型**一个都匹配不到**，而**没有任何检查**。与 TP 的守卫不对称 ——
`parallelize_tensor_parallel` 在 plan 没匹配到模块时会拒绝（"the plan names no module to
shard"），SP 这一侧没有对应物。

实测（守卫加入前），`tp_size=2` + `sp_backend='megatron'`：

| 模型 | 结果 |
|---|---|
| norm 是 `nn.LayerNorm` | 不报错，`norm.weight` 是 `DTensor(Replicate())` |
| norm 是别的类型 | **不报错**，`norm.weight` 仍是普通 `Parameter` —— SP 什么都没做 |

TP plan 匹配到了投影，所以相邻那条守卫没有理由触发。**loss 曲线看不出来**（SP 是激活布局
优化，不影响数值），所以没有任何东西会暴露它。

**修法**：`require_sequence_parallel_targets`，在 `parallelize` 里、**切分 stage 之前**调用。
按**整个模型**判而不是按 stage 判 —— PP 下 embedding-only 的 stage 合法地不含 norm，
按 stage 判会拒绝一个能工作的配置。每个 rank 看到同一个未切分的模型，所以判定一致，
失败是干净的报错而不是某个 rank 挂住。

### 二、RMSNorm：从"拒绝"改成"支持"

`nn.RMSNorm`（Llama 用的那个）现在是支持的，因为**样式本来就与具体 norm 无关**：
它切的是**序列**轴，norm 自己的轴（最后一维）在每个 rank 上都是完整的，所以任何在 hidden 维
归一的模块都适用。

实测（world=2，对 dense 参考）：forward `5.96e-08`、输入梯度 `4.66e-10`、norm 参数梯度
`1.86e-09` —— 都在 fp32 舍入量级。

判断依据不只是数值：**一个什么都不切的样式也会和 dense 完全相等**，所以新用例复用了
LayerNorm 用例的**全部断言**（含"激活是 `Shard` DTensor""本地序列长度减半""optimizer 的
momentum 最终是 `Replicate` 且跨 rank 一致"）。两条路径共用同一个断言体，无法各自漂移。

### 三、PP 的切分注入点

`split_sequential` 用 `named_children()` 既当层列表又当切片边界，所以要求模型 `forward`
是**直接子模块的链**。HuggingFace 的模型不满足：`LlamaForCausalLM` 的直接孩子是
`model`（整个 trunk）和 `lm_head`，四个 transformer 层在 `model.layers` 里再深一层。

**这里要更正一条先前的说法。** 我在评估时写过"切错且不报错"，不准确：`split_sequential`
确实不报错地返回了 `['model'] / ['lm_head']`，但真正跑起来会在 stage 0 第一次被调用时炸：

```
NotImplementedError: Module [Module] is missing the required "forward" function
```

是**迟到的、且不提流水线切分**的错误，不是静默的错误结果。差别重要，因为它决定了守卫该
证明什么。

**修法两条**：

1. `execution_order` 注入点（`split_sequential` / `stage_assignment` / `parallelize` /
   `bootstrap`），签名照 `ModelAdapter.layers` 的形状 —— 收模型、返回有序的 `(name, module)`
   单位，**含 glue**。Llama 的写法：

   ```python
   def llama_execution_order(model):
       return [("embed_tokens", model.model.embed_tokens),
               *[(f"layer_{i}", layer) for i, layer in enumerate(model.model.layers)],
               ("norm", model.model.norm),
               ("lm_head", model.lm_head)]
   ```

   名字必须无点（`PipelineSequence` 用 `add_module` 注册，它拒绝 `"."`），所以带点的
   `"model.layers.0"` 会被挡下并给出明确报错，而不是抛 `nn.Module` 内部的 `KeyError`。

2. `_refuse_unrunnable_units`：默认路径下，**无法运行**的切分直接拒绝。判据不是"看起来不均衡"，
   而是**可证明的**：`PipelineSequence` 逐个 `调用` 子模块，所以没有 `forward` 的孩子
   （裸 `nn.Module`、`nn.ModuleList`）**无论怎么分配到 stage 都跑不起来**。

   **已知局限（未修）**：如果那个容器**自己定义了可用的 `forward`**（HF 的 `LlamaModel`
   就是），框架无法把它和"有意切成两段"区分开，因此不拒绝 —— 结果是整个 trunk 落在
   stage 0。参数计数会显示这一点，`execution_order` 是解法，但没有任何自动检查会拦它。
   这一条是刻意不做的取舍：为了拦住它只能靠猜模型结构，而猜错会拒绝能工作的配置。

### 验收

`verify_parallel.py` world 4：8 个组合 0 失败；`270 passed`；ruff 84（未变）；3 契约。

**一条自我更正**：本节之前的评估里我报过"examples 全部通过"。那个检查是错的 ——
循环里读的是管道末端 `tail` 的退出码而不是 python 的。按正确方式重测：9 个示例文件中 8 个
退出 0，`fsdp_train/train.py` 在单进程下退出 1 并**这是正确的** —— 它要求已初始化的进程组，
docstring 与 README 都写明要用 2 个 rank 启。按文档方式（`WORLD_SIZE=2`，两个进程）复跑：
`completed 4 optimizer steps`，rank 1 无异常。

## TP 默认跑在"本地张量模式"下，任何需要知道自己形状的模型都无法使用（2026-09-17，已修）

发现路径：写使用指南时拿一个 **HF 形状的真实 attention 模型**接 TP，报了一个形状错误。
仓库里 TP 的测试模型是 `Linear + relu`，`tiny_transformer` 没有 attention，
`tp_sp_train` 是单进程的 `UlyssesAttention` —— **没有任何测试让 TP 跑过带 head 重排的模型**。

### 现象

```
model.py: RuntimeError: shape '[2, 8, 4, 8]' is invalid for input of size 256
```

`512` 是全局尺寸，`256` 是本地尺寸 —— 说明被重排的张量是**某个 rank 的切片**。诊断脚本
（`scripts/diag_tp_attention.py`）打出决定性的一行：

```
q_proj.weight      DTensor(32, 32) local=(16, 32) placements=(Shard(dim=0),)   ← 权重分片正确
q_proj(hidden)     Tensor(2, 8, 16) placements=None                            ← 输出是普通张量
```

### 根因

torch 的 `ColwiseParallel` / `RowwiseParallel` 有一个 `use_local_output` 参数，
**默认 `True`**，语义就是一行：

```python
return outputs.to_local() if use_local_output else outputs
```

我们的 plan 从不设置它，所以投影在"把 placement 标好"之后又**把它丢掉**，交给模型一个
形状正常的普通张量。模型没有任何办法察觉自己拿到的是分片，**也无法自救** —— 正确处理需要
把 head 数除以一个它看不见的 `tp_size`。

**为什么现有测试全绿**：本地模式下，列投影与行投影之间的激活就是每 rank 的切片，而
`RowwiseParallel` 把普通张量标注为 `Shard(-1)`、`ColwiseParallel` 标注为 `Replicate()` ——
两者自洽。所以"投影 + 逐元素运算"的链条完全正确，而那正是仓库里所有 TP 测试的形状。
**它在模型需要知道自身形状的那一刻才断**，也就是任何带 head 重排的 attention。

### 修法（两次才对）

**第一次是错的。** 我在两个样式上都设 `use_local_output=False`，结果残差相加直接炸：

```
hidden = hidden + self.self_attn(self.input_layernorm(hidden))
RuntimeError: aten.add.Tensor: got mixed torch.Tensor and DTensor, need to convert
all torch.Tensor to DTensor before calling distributed operators!
```

`hidden` 是 embedding 的输出（没有投影碰过它，是普通张量），而行投影开始返回 `DTensor`。
**残差流不能一半分片**：embedding 和 norm 都不分片，一个返回 `DTensor` 的投影会迫使它们
全部变成并行感知的。

**正确的规则是不对称的**：

* **列投影保留 placement** —— 它的输出正是模型要重排的东西，而只有模型知道那是 head 切分
  而不是特征切分；
* **行投影返回普通张量** —— 它输出前已经 all-reduce 成 `Replicate`，`to_local()` 无损，
  而且它要回到**未分片的残差流**里去。

这样分片被限制在 attention / MLP 区域内，残差流永远看不见它，**模型除了那一处算子外
一行都不用改**。开关是 `TransformerTPPlan.keep_placements`（默认 `True`），
设 `False` 回到旧行为。

### 附带发现：DTensor 没有 SDPA 的分片策略

修好 placement 后下一个错误：

```
NotImplementedError: Operator aten._scaled_dot_product_flash_attention_for_cpu.default
does not have a sharding strategy registered.
```

这是 torch 的算子覆盖问题，不是框架能兜底的 —— **只有模型知道"按 rank 分别算等于整体算"**
（分片分的正是 head 轴，每 rank 持有完整 head，而 attention 逐 head 独立）。所以修法在
模型侧，写成通用的本地往返，用张量自己的 `device_mesh`，**不需要知道 `tp_size`**：

```python
attended = F.scaled_dot_product_attention(q.to_local(), k.to_local(), v.to_local())
return DTensor.from_local(attended, q.device_mesh, [Shard(1)])
```

CUDA 的 flash 变体在 torch 里有注册，所以 GPU 上可能不需要这一步 —— 但没有实测过，
已列入 `docs/GPU验证清单.md`。

### 验收（本机实测）

| 检查 | 结果 |
|---|---|
| `tp`、`tp_sp` 的 loss | 与单卡基准逐位相同 `4.392560 -> 4.332551` |
| `verify_parallel.py --world-size 2` | `4 个组合 · 0 个失败`（确认旧路径未被破坏）|
| 六种策略 | 全部与基准一致 |

`scripts/verify_parallel.py` 的 `tp2` 组合在此之前一直是绿的，**这次改动没有动它** ——
因为它的模型中间只有逐元素运算，本来就不受影响。这正是这个缺陷能长期存在的原因。
